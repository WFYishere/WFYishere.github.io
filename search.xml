<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Welcome to my blog</title>
    <url>/2024/02/22/Welcome-to-my-blog/</url>
    <content><![CDATA[<h1 id="colorcyanwelcome-to-my-blog"><span
class="math inline">\(\color{cyan}{Welcome To My Blog!}\)</span></h1>
<span id="more"></span>
<h1 id="introduction">Introduction</h1>
<p>I've been trying to document things about myself, as a reminder that
I have <strong>existed</strong> in this world.</p>
<p>In this blog, I might write about personal feelings, reading notes,
thoughts, achievements, plans, challenges...etc</p>
<blockquote>
<p>Welcome to my blog!</p>
</blockquote>
<h1 id="have-a-look-around">Have A Look Around!</h1>
<ul>
<li><p>To view <span class="math inline">\(\color{green}{daily
thoughts}\)</span>, click <span
class="math inline">\(\href{https://wfyishere.github.io/categories/thoughts/}{&lt;Here&gt;}\)</span></p></li>
<li><p>To view <span
class="math inline">\(\color{green}{notes}\)</span>, click <span
class="math inline">\(\href{https://wfyishere.github.io/categories/Notes/}{&lt;Here&gt;}\)</span></p></li>
<li><p>To view some <span
class="math inline">\(\color{green}{interesting challenges}\)</span>
that I've done, click <span
class="math inline">\(\href{https://wfyishere.github.io/categories/Challenges/}{&lt;Here&gt;}\)</span></p></li>
<li><p>To view some <span
class="math inline">\(\color{green}{Experiences}\)</span>, click <span
class="math inline">\(\href{https://wfyishere.github.io/categories/Experiences/}{&lt;Here&gt;}\)</span></p></li>
</ul>
<h1 id="introduction-to-functionalities-of-the-website">Introduction to
Functionalities of the Website</h1>
<p><em>It's adviced to view this website on your computer(to fully make
use of the functionalities)</em></p>
<h2 id="sidebar">Sidebar</h2>
<h3 id="menu">Menu</h3>
<p>On the left side of the website, <em>the side bar</em>, you can click
on the different directories on the menu such as <code>About</code>,
<code>Categories</code>, <code>Tags</code>, <code>Archives</code>,
<code>To Do</code>, <code>Search</code></p>
<ul>
<li><p><code>About</code> : Gives basic information about the website
and me.</p></li>
<li><p><code>Categories</code> : Page where you can check the different
types of posts, such as Notes/Thoughts/Challenges etc. Subcategories are
provided.</p></li>
<li><p><code>Tags</code> : Labels different posts such as
"MachineLearning" etc.</p></li>
<li><p><code>Archives</code> : Overview of all the posts on the website
in time order.</p></li>
<li><p><code>To do</code> : Plans for what I want to write / functions
that I want to implement in the future. Feel free to give
advice!</p></li>
<li><p><code>Search</code> : Search specific contents that you want to
look for.</p></li>
</ul>
<h3 id="table-of-contents">Table of Contents</h3>
<p>When you view an article, you can click on the sessions under
<code>Table of Contents</code> to go to different chapters of the
post.</p>
<h3 id="globe">Globe</h3>
<p>If you're using a laptop, you might notice the 'Globe' at the bottom
of the sidebar. This shows the places where all the visitors of the
website came from! Try put your cursor on the globe and zoom in/out
using your mouse! <strong>Each red dot symbolizes one user!</strong></p>
<h2 id="posts">Posts</h2>
<ol type="1">
<li><p>Click on <code>read more</code> to read full content of each
post!</p></li>
<li><p>Word count and estimated Reading time are displayed under the
title of each post.</p></li>
<li><p>If you find my post interesting, feel free to click on the social
media icons at the end of each post to share this post!</p></li>
<li><p>You can login your github account to comment each post.</p></li>
</ol>
<h2 id="bottome-bar">Bottome Bar</h2>
<p>The bottom bar provides information about the total wordcount /
estimated reading time / the days this website has survived.</p>
<p>Thank Next &amp; Hexo for powering this website.</p>
]]></content>
  </entry>
  <entry>
    <title>CS2040S Midterm CheatSheet</title>
    <url>/2024/03/11/CS2040S-Midterm-CheatSheet/</url>
    <content><![CDATA[<p>CheatSheet</p>
<span id="more"></span>
<ol type="1">
<li>ADTs are specifications for behaviors without any implementation.
E.g. Stack, Queue, Dictionary, Set, Map. Data Structures are concrete
implementations of ADTs. E.g. Array, Linked List, Binary Tree, Hash
Table, Heap</li>
</ol>
<h1 id="lecture-3-4-peak-finding-searching">Lecture 3 &amp; 4 : Peak
Finding &amp; Searching</h1>
<ol start="2" type="1">
<li>Applications of binary search: Think of binary search whenever:
<ul>
<li>There is a sequence of "monotonic function" in blackbox. That is ,
for each v[i], for i &lt;= k, it returns false, for i &gt;= k, it
returns true.</li>
</ul></li>
</ol>
<h1 id="sort">Sort</h1>
<p>Stirling approximation: <code>O(log(n!)) == O(nlog(n))</code></p>
<h2 id="classic-sorting-algorithms"><strong>Classic Sorting
Algorithms</strong></h2>
<h3 id="bubblesort">BubbleSort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">repeat n times:</span><br><span class="line">    <span class="keyword">for</span> j ¬ <span class="number">1</span> to n-<span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> A[j] &gt; A[j+<span class="number">1</span>] then <span class="title function_">swap</span><span class="params">(A[j], A[j+<span class="number">1</span>])</span></span><br></pre></td></tr></table></figure>
<h3 id="selection-sort">Selection Sort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> j ¬ <span class="number">1</span> to n-<span class="number">1</span>:</span><br><span class="line">    find minimum element A[j] in A[j..n] <span class="comment">//A[k]</span></span><br><span class="line">swap(A[j], A[k])</span><br></pre></td></tr></table></figure>
<h3 id="insertion-sort">Insertion Sort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">InsertionSort(A, n)</span><br><span class="line">    <span class="keyword">for</span> j ¬ <span class="number">2</span> to n</span><br><span class="line">    key ¬ A[j]</span><br><span class="line">    <span class="comment">//Insert key into the sorted array A[1..j-1]</span></span><br><span class="line">    i ¬ j-<span class="number">1</span></span><br><span class="line">    <span class="keyword">while</span> (i &gt; <span class="number">0</span>) and (A[i] &gt;key)</span><br><span class="line">        A[i+<span class="number">1</span>] ¬ A[i]</span><br><span class="line">        i ¬ i-<span class="number">1</span></span><br><span class="line">    A[i+<span class="number">1</span>] ¬ key</span><br></pre></td></tr></table></figure>
<blockquote>
<p>Average Case Analysis: On average, a key in position j needs to move
j/2 slots backward (in expectation).</p>
</blockquote>
<h3 id="mergesort">MergeSort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">MergeSort(A, n)</span><br><span class="line"><span class="keyword">if</span> (n=<span class="number">1</span>) then <span class="keyword">return</span>;</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    X ¬MergeSort(A[<span class="number">1.</span>.n/<span class="number">2</span>], n/<span class="number">2</span>);</span><br><span class="line">    Y ¬MergeSort(A[n/<span class="number">2</span>+<span class="number">1</span>, n], n/<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">return</span> Merge (X,Y, n/<span class="number">2</span>);</span><br></pre></td></tr></table></figure>
<p>For the iterative MergeSort, we will sort the array in groups of
power of 2. In other words, we will first sort the arrays in pairs, then
merge into 4’s, 8’s and so on, until we have merged the entire array</p>
<h2 id="summary"><strong>Summary</strong></h2>
<table>
<colgroup>
<col style="width: 29%" />
<col style="width: 16%" />
<col style="width: 16%" />
<col style="width: 23%" />
<col style="width: 14%" />
</colgroup>
<thead>
<tr class="header">
<th>Features</th>
<th>Bubble Sort</th>
<th>Selection Sort</th>
<th>Inerstion Sort</th>
<th>Merge Sort</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Time Complexity</td>
<td>O(n^2)</td>
<td>O(n^2)</td>
<td>O(n^2)</td>
<td>O(n * log(n))</td>
</tr>
<tr class="even">
<td>NearlySorted ? Random</td>
<td>O(n) &lt;</td>
<td>=</td>
<td>O(n) &lt;</td>
<td>=</td>
</tr>
<tr class="odd">
<td>Space Complexity</td>
<td>O(n)</td>
<td>O(n)</td>
<td>O(n)</td>
<td>O(n) (No need to allocate new arrays, can you previous ones instead)
/ O(n * log(n)) when it's MergeSort iteration</td>
</tr>
<tr class="even">
<td>Stability</td>
<td>1(swap only when elements are different)</td>
<td>0(e.g. [2, 2, 1])</td>
<td>1(A[i] &gt; key not &gt;=)</td>
<td>1(if implementation is designed carefully)</td>
</tr>
<tr class="odd">
<td>Behaviour</td>
<td>Largest 𝑘 elements are at the end of the array(after 𝑘
iterations)</td>
<td>Smallest 𝑘 elements are at the start of the array(after 𝑘
iterations)</td>
<td>First 𝑘 elements are sorted (after 𝑘 iterations),last 𝑛 − 𝑘 elements
are untouched</td>
<td>Each subarray is already sorted when merging</td>
</tr>
</tbody>
</table>
<h2 id="quicksort">Quicksort</h2>
<h3 id="introduction">Introduction</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">QuickSort(A[<span class="number">1.</span>.n], n)</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">1</span>) then <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        Choose pivot index pIndex.</span><br><span class="line">        p = partition(A[<span class="number">1.</span>.n], n, pIndex)</span><br><span class="line">        x = QuickSort(A[<span class="number">1.</span>.p–<span class="number">1</span>], p–<span class="number">1</span>)</span><br><span class="line">        y = QuickSort(A[p+<span class="number">1.</span>.n], n–p)</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">partition(A[<span class="number">1.</span>.n], n, pIndex) <span class="comment">// Assume no duplicates, n&gt;1</span></span><br><span class="line">    pivot = A[pIndex]; <span class="comment">// pIndex is the index of pivot</span></span><br><span class="line">    swap(A[<span class="number">1</span>], A[pIndex]); <span class="comment">// store pivot in A[1]</span></span><br><span class="line">    low = <span class="number">2</span>; <span class="comment">// start after pivot in A[1]</span></span><br><span class="line">    high = n+<span class="number">1</span>; <span class="comment">// Define: A[n+1] = ¥</span></span><br><span class="line">    <span class="keyword">while</span> (low &lt; high)</span><br><span class="line">        <span class="keyword">while</span> (A[low] &lt;= pivot) and (low &lt; high) <span class="keyword">do</span> low++;</span><br><span class="line">        <span class="keyword">while</span> (A[high] &gt; pivot) and (low &lt; high) <span class="keyword">do</span> high– – ;</span><br><span class="line">        <span class="keyword">if</span> (low &lt; high) then <span class="title function_">swap</span><span class="params">(A[low], A[high])</span>;</span><br><span class="line">    swap(A[<span class="number">1</span>], A[low–<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">return</span> low–<span class="number">1</span>;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ParanoidQuickSort(A[<span class="number">1.</span>.n], n)</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">1</span>) then <span class="keyword">return</span>;    </span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="type">repeat</span></span><br><span class="line">            <span class="variable">pIndex</span> <span class="operator">=</span> random(<span class="number">1</span>, n)</span><br><span class="line">            p = partition(A[<span class="number">1.</span>.n], n, pIndex)</span><br><span class="line">        until p &gt; (<span class="number">1</span>/<span class="number">10</span>)n and p &lt; (<span class="number">9</span>/<span class="number">10</span>)<span class="type">n</span></span><br><span class="line"></span><br><span class="line">    <span class="variable">x</span> <span class="operator">=</span> QuickSort(A[<span class="number">1.</span>.p–<span class="number">1</span>], p–<span class="number">1</span>)</span><br><span class="line">    y = QuickSort(A[p+<span class="number">1.</span>.n], n–p)</span><br></pre></td></tr></table></figure>
<h3 id="quicksort-optimization">Quicksort Optimization</h3>
<ol type="1">
<li><p>Switch to InsertionSort for small arrays.</p></li>
<li><p>Halt recursion early, leaving small arrays unsorted. Then perform
InsertionSort on entire array. (InsertionSort is fast on almost sorted
arrays, about O(n))</p></li>
</ol>
<h2 id="order-statistics">Order Statistics</h2>
<p>Find <span class="math inline">\(k^{th}\)</span> smallest element in
an unsorted array</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Select(A[<span class="number">1.</span>.n], n, k)</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">1</span>) then <span class="keyword">return</span> A[<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">else</span> Choose random pivot index pIndex.</span><br><span class="line">        p = partition(A[<span class="number">1.</span>.n], n, pIndex)</span><br><span class="line">        <span class="keyword">if</span> (k == p) then </span><br><span class="line">            <span class="keyword">return</span> A[p];</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (k &lt; p) then</span><br><span class="line">            <span class="keyword">return</span> Select(A[<span class="number">1.</span>.p–<span class="number">1</span>], k)</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (k &gt; p) then</span><br><span class="line">            <span class="keyword">return</span> Select(A[p+<span class="number">1</span>], k – p)</span><br></pre></td></tr></table></figure>
<p><strong>Time Complexity: O(n)</strong></p>
<h1 id="lecture-08-trees">Lecture 08 Trees</h1>
<h2 id="dynamic-data-structures">Dynamic Data Structures</h2>
<h2 id="tree-basic-concepts">Tree Basic Concepts</h2>
<ol type="1">
<li><p>What determines shape? • Order of insertion • Does each order
yield a unique shape? NO – # ways to order insertions: n! – # shapes of
a binary tree? ~4n (Catalan numbers: Catalan Numbers Cn = # of trees
with (n+1) nodes Cn = # expressions with n pairs of matched
parentheses)</p></li>
<li><p>Successor:</p>
<p>If you search for a key not in a tree, you would either find its
successor or its predecessor <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Successor(<span class="type">int</span> key) &#123; <span class="comment">// successor function for key (in / not in tree)</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> search(key); <span class="comment">//either return successor or predecessor</span></span><br><span class="line">    <span class="keyword">if</span>(result &gt; key) <span class="keyword">return</span> result;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> SuccessorKeyInTree(result);</span><br><span class="line">&#125;</span><br><span class="line">SuccessorKeyInTree(<span class="type">int</span> key) &#123;<span class="comment">// search for successor when key is in tree</span></span><br><span class="line">    <span class="keyword">if</span> (key has right child ) &#123;</span><br><span class="line">        <span class="keyword">return</span> rightChild;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> ((parent != <span class="literal">null</span>) &amp;&amp; (child == parent.rightTree)) &#123;</span><br><span class="line">        child = parent;</span><br><span class="line">        parent = child.parentTree; <span class="comment">// note that here child has become parent</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> parent;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></li>
<li><p>Deleting <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (key.childNumber == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">//simply delete.</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (key.childNumber == <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="comment">//make the child of key substitude child</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">//key has two child -&gt; key has right child -&gt; the successor is the min in the right child -&gt; sucessor has no left child.</span></span><br><span class="line">    substitude successor with its child and substitude key with successor.</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></li>
</ol>
<h2 id="keeping-trees-balanced">Keeping Trees Balanced</h2>
<h3 id="concept">Concept</h3>
<p>Key definition: A tree is <strong>balanced</strong> if h = O(logN)
(By default: log(n) –1 ≤ h ≤ n)</p>
<h3 id="avl-trees">AVL Trees</h3>
<h4 id="proof-of-balance">Proof of Balance</h4>
<ol type="1">
<li><p>node v is height-balanced if |v.left.height – v.right.height|
&lt;= 1</p></li>
<li><p>A height-balanced tree with n nodes has at most height h &lt;
2log(n)</p></li>
</ol>
<p><em>Interesting Property: a maximal imbalanced AVL tree(AVL treewith
the minimum possible number of nodes given its height h.)has all its
subtrees to be maximally imbalanced.</em></p>
<h4 id="maintaining-height-balance">Maintaining Height-Balance</h4>
<figure>
<img src="RightRotation.png" style="width:30.0%" alt="RightRotation" />
<figcaption aria-hidden="true">RightRotation</figcaption>
</figure>
<p>After insertion, start at bottom, work your way up.</p>
<p>Assume A is the lowest node violating valance property. Assuming that
A is <strong>left heavy</strong>:</p>
<ol type="1">
<li>B is balanced</li>
</ol>
<figure>
<img src="TreeRotationCase1.png" style="width:30.0%"
alt="B is balanced: do righ rotation on A" />
<figcaption aria-hidden="true">B is balanced: do righ rotation on
A</figcaption>
</figure>
<ol start="2" type="1">
<li>B is left-heavy</li>
</ol>
<figure>
<img src="TreeRotationCase2.png" style="width:30.0%"
alt="B is left-heavy" />
<figcaption aria-hidden="true">B is left-heavy</figcaption>
</figure>
<ol start="3" type="1">
<li>B is right heavy</li>
</ol>
<figure>
<img src="TreeRotationCase3_1.png" style="width:30.0%"
alt="Left Rotate B - A and C are still out of balance" />
<figcaption aria-hidden="true">Left Rotate B - A and C are still out of
balance</figcaption>
</figure>
<figure>
<img src="TreeRotationCase3_2.png" style="width:30.0%"
alt="Right Rotate A - Solved!" />
<figcaption aria-hidden="true">Right Rotate A - Solved!</figcaption>
</figure>
<p>[S] what about nodes higher than that?</p>
<pre><code>- In Case 2 &amp; 3, we reduce the height by 1, thus the upper must be balanced.

- In Case 1, Case 1 would never appear when doing an insertion.</code></pre>
<p>[S] What about deletion!</p>
<p>For deletion, we walk up tree, check for balance and rotate every
node if needed. It is NOT sufficient to only fix lowest out-of-balance
node.</p>
<figure>
<img src="BalanceButNotHeightBalance.png" style="width:30.0%"
alt="[U] Why?" />
<figcaption aria-hidden="true">[U] Why?</figcaption>
</figure>
<h2 id="augmenting-trees">Augmenting Trees</h2>
<h3 id="dynamic-order-statistics">Dynamic Order Statistics</h3>
<ol type="1">
<li><p>Find the rank of a node <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">rank(node)</span><br><span class="line">    rank = node.left.weight + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span> (node != <span class="literal">null</span>) <span class="keyword">do</span></span><br><span class="line">        <span class="keyword">if</span> node is left child then</span><br><span class="line">            <span class="keyword">do</span> nothing</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> node is right child then</span><br><span class="line">            rank += node.parent.left.weight + <span class="number">1</span>;</span><br><span class="line">        node = node.parent;</span><br><span class="line">    <span class="keyword">return</span> rank;</span><br></pre></td></tr></table></figure></p></li>
<li><p>Maintaining weight during insertion and rotation.</p></li>
</ol>
<figure>
<img src="MaintainingWeight.png" style="width:30.0%"
alt="Maintaining weight during rotations" />
<figcaption aria-hidden="true">Maintaining weight during
rotations</figcaption>
</figure>
<h3 id="trie">Trie</h3>
<ul>
<li>Costs(h for the height of trie, L for the length of word)
<ul>
<li>search: time - O(L)</li>
<li>store: space - O(L)</li>
</ul></li>
</ul>
<h3 id="interval-queries">Interval Queries</h3>
<h4 id="goal">Goal:</h4>
<ul>
<li>Solutions where space is linear (or near linear) in the number of
things stored (i.e., intervals).</li>
<li>Operations are logarithmic in number of things stored.</li>
</ul>
<h4 id="augment">Augment</h4>
<p>store the maximum of right end point!</p>
<h4 id="implement">Implement</h4>
<h5 id="search">search:</h5>
<figure>
<img src="SearchIntervalTree.png" style="width:30.0%"
alt="sample interval tree" />
<figcaption aria-hidden="true">sample interval tree</figcaption>
</figure>
<p>when we walk down the nodes to(17,19), the information that we need
are: 17(left interval), 25(max = left subtree)</p>
<ul>
<li><p>Case 1: x &gt; 25 (max of left subtree):</p>
<p>Go right. (the node is either in right tree or not in tree!)</p></li>
<li><p>Case2: 17 &lt; x &lt;= 25</p>
<p>Go left. (there must be atleast one node that contains x!)</p></li>
<li><p>Case3: x &lt; 17</p>
<p>Go left. (there is 0 possibility that the right subtree contains
x!)</p></li>
</ul>
<p>In three cases we are safe to go (the direction we go contains the
key / the direction we don't go does not contain key)</p>
<h3 id="orthogonal-range-searching">Orthogonal Range Searching</h3>
<h4 id="introduction-1">Introduction</h4>
<p>Are there any good restaurants within one block of me - search for
the points in a given box</p>
<h4 id="sample-one-dimension">Sample: One-Dimension</h4>
<h5 id="strategy">Strategy</h5>
<ol type="1">
<li>Use a binary search tree and store all the points in the leaves of a
tree. Each internal node stores the MAX of any leaf in the left
sub-tree.</li>
</ol>
<figure>
<img src="OneDimensionORS.png" style="width:30.0%" alt="Sample Tree" />
<figcaption aria-hidden="true">Sample Tree</figcaption>
</figure>
<ol start="2" type="1">
<li><p>Do Search(10, 50): (Time complexity: <strong>O(log(n) +
k)</strong>, where k is the number of items)</p>
<ul>
<li>Find Split Node: the highest node that search includes both left and
right subtrees(in this case, 25) <strong>O(logn)</strong></li>
<li>Do left and right traversal on the left and right tree.
<strong>O(k)</strong></li>
<li>Left Traversal: The search interval for a left-traversal at node v
includes the maximum item in the left subtree of v. <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LeftTraversal(v, low, high) &#123;</span><br><span class="line">    <span class="keyword">if</span> (low &lt;= v.key) &#123;</span><br><span class="line">        all-leaf-traversal(v.right); </span><br><span class="line">        <span class="comment">//change all-tree-traversals to total += v.right.ount</span></span><br><span class="line">        <span class="comment">//if just need to know how many nodes are with the range</span></span><br><span class="line">        LeftTraversal(v.left, low, high);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">// left traversal on the right subtree</span></span><br><span class="line">        LeftTraversal(v.right, low, high);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">RightTraversal(v, low, high) &#123;</span><br><span class="line">    <span class="keyword">if</span> (v.key &lt;= high) &#123;</span><br><span class="line">        all-leaf-traverasal(v.left);</span><br><span class="line">        RightTraversal(v.right, low, high);</span><br><span class="line">    &#125;  <span class="keyword">else</span> &#123;</span><br><span class="line">        RightTraversal(v.left, low, high);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul></li>
<li><p>BuildTree time complexity : O(n log n)</p></li>
<li><p>Dynamic updates: Rotation</p></li>
</ol>
<h4 id="two-dimension">Two-Dimension</h4>
<ol type="1">
<li>Idea:</li>
</ol>
<ul>
<li>1: Create a 1d-range-tree on the x-coords.</li>
<li>2: Store a y-tree at each x-node containing all the points in the
sub-tree.</li>
</ul>
<figure>
<img src="TwoDimensionRangeSearching.png" style="width:30.0%"
alt="Too much space for all the nodes! - use two-dimensional Trees!" />
<figcaption aria-hidden="true">Too much space for all the nodes! - use
two-dimensional Trees!</figcaption>
</figure>
<h3 id="k-d-tree">k-d Tree</h3>
<p>space partitioning data structure for organising points into a
𝑘-dimensional space.</p>
<p>The even stores horizontal splits and odd stores vertical split(vice
versa)</p>
<h3 id="a-b-trees">(a, b) trees</h3>
<h4 id="rules">Rules</h4>
<ul>
<li>2 ≤ a ≤ (b + 1)/2, where a and b refers to the minimum and maximum
number of <strong>children</strong> in a internal node(not root /
leaf).</li>
</ul>
<figure>
<img src="abtreeChildPolicy.png" style="width:30.0%"
alt="(a,b)tree child policy" />
<figcaption aria-hidden="true">(a,b)tree child policy</figcaption>
</figure>
<ul>
<li>every node should follow that: range theory</li>
<li>All leaves should have the same depth.</li>
</ul>
<h4 id="manipulation">Manipulation</h4>
<ol type="1">
<li><p>Is ab tree balanced? <img src="abtreeBalance.png"
style="width:30.0%" alt="Is ab tree balanced?" /></p></li>
<li><p>Insertion:</p></li>
</ol>
<p>Solution 1: (1) At first put the element in its correct place in the
node (which is in sorted order)</p>
<ol start="2" type="1">
<li><p>If the node overflows, then find the median ( = if is even and if
is odd ) key in that node.</p></li>
<li><p>Put the median key in the parent and split the current
node</p></li>
</ol>
<p>Solution 2: Proactive Approach</p>
<p>Preemptively split a node whe it is full so that it does not overflow
in the future.</p>
<ol start="3" type="1">
<li>Deletion:</li>
</ol>
<ol type="1">
<li>Case 1: Problem with Orphan Children.</li>
</ol>
<figure>
<img src="abtreeOrphan.png" style="width:30.0%" alt="Delete 13" />
<figcaption aria-hidden="true">Delete 13</figcaption>
</figure>
<pre><code>In this case, we replace the deleted node either with the predecessor or successor key</code></pre>
<ol start="2" type="1">
<li>Case 2: Problem with downflow</li>
</ol>
<figure>
<img src="abtreeDownFlow.png" style="width:30.0%" alt="Delete 23" />
<figcaption aria-hidden="true">Delete 23</figcaption>
</figure>
<p>If at least one sibling has at least a keys, then donate the key to
the other sibling</p>
<p>If none have enough keys, then merge the two siblings and the parent
together.</p>
<figure>
<img src="abtreeDownFlow2.png" style="width:30.0%" alt="Downflow" />
<figcaption aria-hidden="true">Downflow</figcaption>
</figure>
<figure>
<img src="abtreeDownFlow3.png" style="width:30.0%"
alt="Down flow again" />
<figcaption aria-hidden="true">Down flow again</figcaption>
</figure>
<figure>
<img src="abtreeDownFlow4.png" style="width:30.0%"
alt="Another example" />
<figcaption aria-hidden="true">Another example</figcaption>
</figure>
<p><img src="abtreeDownFlow5.png" style="width:30.0%" /></p>
<p><img src="abtreeDownFlow6.png" style="width:30.0%" /></p>
<h4 id="b-trees">B-trees</h4>
<p>B-trees are simply (B, 2B)-trees. That is to say, they are a
subcategory of (a, b)-trees such that a = B and b = 2B.</p>
<ol type="1">
<li>Cost</li>
</ol>
<ul>
<li><p>All the costs of searching a key list / splitting a node /
merging / sharing B-tree nodes are O(1)!</p></li>
<li><p>The cost of searching a B-tree / inserting / deleting in a B tree
is O(loga n)!</p></li>
</ul>
<h2 id="tree-operation-conclusion">Tree Operation conclusion</h2>
<ol type="1">
<li>modifying:
<ul>
<li>insert: O(h)</li>
<li>delete: O(h)</li>
</ul></li>
<li>Query Operations:
<ul>
<li>search: O(h)</li>
<li>predecessor / successor: O(h)</li>
<li>findMax / findMin: O(h)</li>
<li>Traversal: O(n)</li>
</ul></li>
</ol>
<h2 id="height-of-binary-tree-after-subtree-removal">Height of Binary
Tree After Subtree Removal</h2>
<ul>
<li>Solution 2: <em>Depth of a node refers to the number of edges in the
simple path from root to the node</em>
<ol type="1">
<li>preprocess the tree to get its depth using DFS and height using
in-order-traversal.</li>
<li>For ith query, we look at all the nodes with the same depth of ith
node and find the maximum height among them. The answer to the query
would be <code>node[i].depth + max_height</code>.</li>
<li>SO how do we group the nodes of the same depth? We do this by
iterating the nodes in in-order-traversal([U] wut?). For each group of
nodes in the same depth, we only require the maximum height and second
maximum height. To do this we need O(n) time([U] wut?).</li>
<li>In this algorithm, we only need O(1) for each query. The overal time
complexity would be O(n + m)</li>
</ol></li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>MA2104 Multivariable Calculus</title>
    <url>/2024/03/05/MA2104-Multivariable-Calculus/</url>
    <content><![CDATA[<ol type="1">
<li>[U] It can be proved from the defifinition that if lim(x,y)→(a,b)
f(x, y) = L then • its value L is unique, and • L is independent of the
choice of path approaching (a, b)</li>
</ol>
]]></content>
  </entry>
  <entry>
    <title>CS2030S Lecture Notes</title>
    <url>/2024/03/02/CS2030S-Lecture-Notes/</url>
    <content><![CDATA[<p>CS2030S lecture notes. <span id="more"></span></p>
<h3 id="java">Java</h3>
<ol type="1">
<li><p>Java Compiler: compiled into bytecode.(.java-&gt; javac -&gt;
.class); interpreted and compiled on the fly by JVM into machine
code.(java .class) / be interpreted by the Java interpreter.</p></li>
<li><p>Types:</p></li>
</ol>
<ul>
<li>statically typed language: declare every variable we use in the
program and specify its type(compile-time type).</li>
<li>Primitive: numeric &amp; boolean values byte &lt;: short &lt;: int
&lt;: long &lt;: float &lt;: double; char &lt;: int</li>
<li>Reference Types: stores only the reference to the value</li>
<li>widening type conversion: long i = int(j); narrowing type
conversion: type casting</li>
</ul>
<ol start="3" type="1">
<li>Principals:</li>
</ol>
<ul>
<li>encapsulation: keeping all the data and functions operating on the
data related to a composite data type together within an abstraction
barrier.</li>
<li>"Tell, Don't Ask" principle. The client should tell a Circle object
what to do , instead of asking "What is your radius?" to get the value
of a field</li>
<li>composition: combining classes</li>
<li>Composition : has-a relationship;Inheritance : is-a
relationship.</li>
<li>polymorphism: method invoked is decided during run-time(dynamic
binding/dispatch) - <em>Class Methods do not support dynamic
binding.</em> At compile time, determines the <strong>method
descriptor</strong>(including return type) of the method invoked, using
the compile-time type of the target. At runtime, the method descriptor
is retrieved and the run-tim type is determined</li>
<li>Inheritance: subtypes can only access public fields/methods</li>
<li>Liskov Substitution Principle: A subclass should not break the
expectations set by the superclass</li>
</ul>
<ol start="4" type="1">
<li><p>package: <code>import java.lang.Math;</code>
<code>return Math.PI * this.r * this.r;</code></p></li>
<li><p>The main method <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></li>
<li><p>aliasing: sharing references. Avoid that.e.g.:
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">Point</span> <span class="variable">p</span> <span class="operator">=</span> ...; </span><br><span class="line">Circle1.setPoint(p); </span><br><span class="line">Circle2.setPoint(p); <span class="comment">// Circle1.point is an alias of p</span></span><br></pre></td></tr></table></figure></p></li>
<li><p>Stack and Heap</p></li>
</ol>
<ul>
<li>heap : objects - whenever you use the keyword new</li>
<li>Stack: variables in method call frames(main/moveTo...)</li>
<li>non-static methods, including constructors, have
<code>this</code></li>
<li>Initialization: object- null; Primitive - zero(uninitialized -
∅)</li>
</ul>
<figure>
<img src="StackAndHeap.png"
alt="Circle c; c = new Circle(new Point(1, 2), 3)" />
<figcaption aria-hidden="true">Circle c; c = new Circle(new Point(1, 2),
3)</figcaption>
</figure>
<figure>
<img src="StackAndHeap2.png"
alt="Point p1 = new Point(0, 0);Point p2 = new Point(1, 1);p1.distanceTo(p2)" />
<figcaption aria-hidden="true">Point p1 = new Point(0, 0);Point p2 = new
Point(1, 1);p1.distanceTo(p2)</figcaption>
</figure>
<ol start="8" type="1">
<li><p>Representing a method: <code>Object::equals(Object)</code> - a
method in the class Object</p></li>
<li><p>Method override and overload:</p></li>
</ol>
<ul>
<li><p><code>@Override</code>: Acts as a hint to the compiler. Use
<code>super</code> to call the overriden method.</p></li>
<li><p>method overloading ： differing method signature.</p></li>
</ul>
<ol start="10" type="1">
<li><p><code>obj instanceof Circle</code> (runtime)</p></li>
<li><p><code>final</code></p></li>
</ol>
<ul>
<li><code>final class Circle&#123;&#125;</code> To prevent being inhereited</li>
<li><code>public final boolean contains(Point p)</code> To prevent being
overriden</li>
</ul>
<ol start="13" type="1">
<li><code>abstract</code>: cannot be instantiated - concrete class</li>
<li>Interface:</li>
</ol>
<ul>
<li>across different class hierarchies.(public abstract by default.) An
interface is a type(a type can have multiple supertypes.e.g. a class can
be subype of multiple interface) <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">GetAreable</span> &#123;<span class="type">double</span> <span class="title function_">getArea</span><span class="params">()</span>;&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Flat</span> <span class="keyword">extends</span> <span class="title class_">RealEstate</span> </span><br><span class="line"><span class="keyword">implements</span> <span class="title class_">GetAreable</span>,getMonyable &#123;&#125;</span><br></pre></td></tr></table></figure></li>
<li>casting unrelated interface is allowed. E.g.: class A, Interface B,
class C extends A implements B. There is possiblity that C(subclass of
A) can be casted on B. In this situation <code>A = B</code> compiles.
default implementation: <code>default</code> (impure interfaces)</li>
</ul>
<ol start="17" type="1">
<li>Wrapper Classes : <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">Integer</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">4</span>;</span><br><span class="line"><span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> i;<span class="comment">//Autoboxing &amp; unboxing</span></span><br></pre></td></tr></table></figure> primitive wrapper class objects are
immutable</li>
<li>variance of types:
<ul>
<li>covariant : S&lt;:T -&gt; C(S) &lt;: C(T); contravariant;invariant:
neither <em>Arrays of primitive types are invariant</em>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object[] objArray = intArray;</span><br><span class="line">objArray[<span class="number">0</span>] = <span class="string">&quot;Hello!&quot;</span>; <span class="comment">// &lt;- compiles!</span></span><br></pre></td></tr></table></figure></li>
</ul></li>
<li>Exceptions</li>
</ol>
<ul>
<li><p>instances that are a subtype of the Exception. Follows the
hierarchy <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">InvalidOperandException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    InvalidOperandException(<span class="type">char</span> operator) &#123;</span><br><span class="line">        <span class="built_in">super</span>(<span class="string">&quot;ERROR: Invalid operand for operator &quot;</span> + operator);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="title function_">Circle</span><span class="params">(Point c, <span class="type">double</span> r)</span> <span class="keyword">throws</span> IllegalArgumentException &#123;</span><br><span class="line">    <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;radius is negative.&quot;</span>);</span><br><span class="line">    &#125;<span class="comment">// throw statement causes the method to immediately return.</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  scanner = <span class="keyword">new</span> <span class="title class_">Scanner</span>(reader);</span><br><span class="line">  value = scanner.nextInt();</span><br><span class="line">&#125; <span class="keyword">catch</span> (FileNotFoundException | InputMismatchException) &#123;</span><br><span class="line">  System.err.println(<span class="string">&quot;Unable to find&quot;</span> + e.getMessage);</span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;<span class="comment">// executed even when return or throw is called </span></span><br><span class="line">    scanner.close();</span><br><span class="line">&#125;<span class="comment">//note the throw and throws difference</span></span><br></pre></td></tr></table></figure></p></li>
<li><p>Checked: programmer's errors. Would not happen if perfect code
was written;</p></li>
<li><p>Unchecked:<code>InvalidInputException</code>, subclass of
<code>RuntimeException</code> <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">catch</span> (Exception e) &#123;<span class="comment">// do nothing&#125;//Pokemon Exception Handling:</span></span><br></pre></td></tr></table></figure></p></li>
</ul>
<ol start="20" type="1">
<li>only reference types can be used as type arguments.
<code>Comparable&lt;T&gt;</code> dictates the implementation of int
<code>compareTo(T t)</code> <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class DictEntry&lt;S <span class="keyword">extends</span> <span class="title class_">Comparable</span>&lt;S&gt;&gt; </span><br><span class="line"><span class="keyword">implements</span> <span class="title class_">Comparable</span>&lt;Pair&lt;S,String&gt;</span><br></pre></td></tr></table></figure></li>
</ol>
<ul>
<li><p>Note that we use extends for interface bounding</p></li>
<li><p>the constructor for a generic class is still declared as Pair
(without the type parameters).</p></li>
<li><p>Once a generic type is instantiated, it is called a parameterized
type</p></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Seq</span>&lt;T&gt; &#123;</span><br><span class="line">  <span class="keyword">private</span> T[] array;</span><br><span class="line">  <span class="keyword">public</span> <span class="title function_">Seq</span><span class="params">(<span class="type">int</span> size)</span> &#123;</span><br><span class="line">  <span class="comment">// SuppressWarnings only be used on instantiating expression</span></span><br><span class="line">  <span class="meta">@SuppressWarnings(&quot;unchecked&quot;)</span></span><br><span class="line">    T[] a = (T[]) <span class="keyword">new</span> <span class="title class_">Object</span>[size];</span><br><span class="line">  <span class="built_in">this</span>.array = a;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">(<span class="type">int</span> index, T item)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.array[index] = item;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> T <span class="title function_">get</span><span class="params">(<span class="type">int</span> index)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.array[index];</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">copyFrom</span><span class="params">(Seq&lt;? extends T&gt; src)</span> ;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">copyTo</span><span class="params">(Seq&lt;? <span class="built_in">super</span> T&gt; dest)</span> ;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span> &#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> &lt;S&gt; <span class="type">boolean</span> <span class="title function_">contains</span><span class="params">(Seq&lt;? extends S&gt; seq, S obj)</span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; seq.getLength(); i++) &#123;</span><br><span class="line">      <span class="type">S</span> <span class="variable">curr</span> <span class="operator">=</span> seq.get(i);</span><br><span class="line">      <span class="keyword">if</span> (curr.equals(obj))&#123;<span class="keyword">return</span> <span class="literal">true</span>;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125; <span class="comment">// have to have new genric type in static classes</span></span><br><span class="line">&#125;<span class="comment">//because static classes can be used without instantiation.</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Box</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">equals</span><span class="params">(Object other)</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (other <span class="keyword">instanceof</span> Box&lt;?&gt;) &#123;</span><br><span class="line">        Box&lt;?&gt; otherBox = (Box&lt;?&gt;) other; <span class="comment">//remember to add this!!!</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span>.content.equals(otherBox.getContent())) </span><br><span class="line">          &#123;<span class="keyword">return</span> <span class="literal">true</span>;&#125;</span><br><span class="line">    &#125;&#125;&#125;</span><br></pre></td></tr></table></figure>
<ol start="21" type="1">
<li>Type erasure: after type checking, erase the type parameters and
type arguments during compilation(to Object). If the bounded, replaced
by the bounds. After type erasure, we do explicit typecast on an
object.</li>
</ol>
<ul>
<li><p>arrays are reifiable type(gets full type information during
run-time); but generics are not due to type erasure</p></li>
<li><p>Actually, generic array declaration is fine but generic array
instantiation is not. <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="title class_">Pair</span>&lt;S,T&gt;[<span class="number">2</span>];<span class="comment">//illegal</span></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">T</span>[<span class="number">2</span>];<span class="comment">//illegal</span></span><br><span class="line">T[] array;<span class="comment">//allowed</span></span><br></pre></td></tr></table></figure></p></li>
</ul>
<ol start="22" type="1">
<li><p>ArrayList: a generic class,checks appropriate types during
compile time. Note that it is <strong>invariant</strong>.</p></li>
<li><p><code>@SuppressWarning</code> to the most limited scope</p>
<p>raw types: only used in instance of</p></li>
<li><p>Wild Cards:</p></li>
</ol>
<ul>
<li><p><code>public void copyTo(Seq&lt;? super T&gt; dest)</code>
<em>super is a contravariance type</em></p></li>
<li><p>PECS: Producer Extends Consumer Super</p></li>
<li><p><em>Seq&lt;Object&gt; is NOT supertype of all Seq&lt;T&gt; due to
generics being invariant.</em></p></li>
<li><p><code>Seq&lt;?&gt;</code> is a sequence of objects of some
specific, but unknown type;</p></li>
<li><p><code>Seq&lt;Object&gt;</code> is a sequence of Object instances,
with type checking by the compiler;(Note that generics are invariants so
<code>Seq&lt;Object&gt; !&gt;: Seq&lt;Integer&gt;</code>)</p></li>
<li><p><code>Seq</code> is a sequence of Object instances, without type
checking.</p></li>
</ul>
<ol start="25" type="1">
<li>Type Infering: T &lt;: Type2, then T is inferred as Type2</li>
</ol>
<h2 id="sample-code-for-pe">Sample Code for PE</h2>
<ol type="1">
<li><p>remember to make all the numbers encountered static.
(<code>private static final int RATE = 22</code>)</p></li>
<li><p>Recommended to do(in subclass and superclass)
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Todo:</span></span><br><span class="line">superclass: &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.name; </span><br><span class="line">        <span class="comment">//recommend this. even if there is no conflicting parameter</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">subclass: &#123;</span><br><span class="line">    <span class="keyword">private</span> String typeName;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.typeName + <span class="built_in">super</span>.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// rather than use typeToString in subclass and </span></span><br><span class="line"><span class="comment">// toString and (abstract)typeToString in superclass</span></span><br></pre></td></tr></table></figure></p></li>
<li><p>Vim <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">/ + search <span class="keyword">for</span> word</span><br><span class="line">&#123; + line number</span><br><span class="line">:split / :vsplit</span><br><span class="line">Ctrl + w + w/W</span><br></pre></td></tr></table></figure></p></li>
<li><p>Coding Style</p></li>
</ol>
<ul>
<li><code>public protected private abstract default static final transient volatile synchronized native strictfp</code></li>
</ul>
]]></content>
  </entry>
  <entry>
    <title>Thoughts for Attending Guest Lecture on Pi</title>
    <url>/2024/02/20/Thoughts-for-Attending-Guest-Lecture-on-Pi/</url>
    <content><![CDATA[<p>Attending a guest lecture is fun <span id="more"></span> # Thoughts</p>
<p>I never thought about attending guest lectures before. Mainly because
all the guest lectures that I attended in high school didn't talk much
about "knowledge", but rather about "the history of knowledge" or the
guests' experiences. Since we needed to compete for Olympiad and prepare
for College Entrance Exams, we didn't have much time for doing "useless"
stuff other than studying directly for the exams(at least, that's what I
thought, I'll probably write about that as well in another post. <em>If
I have time</em>). So I've always been kind of resistant to guest
talks.</p>
<p>But then, I've started to change my mind since I came to university
in a different country and tried to touch on things that I didn't want
to take before(I'll also talk about that in the other post). Plus, I
read Cal Newport's <em>How To Win At College</em> (I know this book
sounds uitilitarian but it's actually good), in which he suggested
listening to guest lectures twice a week. So I've decided to give it a
try on a Maths Talks, since I want to know whether my interest actually
lies in this realm.</p>
<p>The talk was given by prof Chan Heng Huat, from the Department of
Mathematics in NUS. There were also several other profs who attended the
meeting such as my prof from GEA1000N (What a surprise that I would be
seeing him tmr) and profs from non-mathematics departments. Apart from
this, several kids were also there in the talk, listening carefully and
silently. That actually surprised me and I was even kind of jealous of
those kids who get the chance to get exposed to these interesting talks
when they were young(maybe one of them would be the next Ramanujan, who
knows!).</p>
<p>Prof Chan mainly talked about the progress and various kinds of
techniques or thoughts that were related to apporximating Pi. He used
simple language, but dived deep into the thought progress of doing these
approximations and gave useful details about the questions that were
raised during the talk. The talk lasted for 40 minutes. It wasn't useful
in a sense that I would probably forget about all these series and
integrations next morning when I wake up. However, I would definetely
say that it makes me remember why and how I loved Mathematics when I was
young. I can't sum up <strong>now</strong> about what the talk brought
to me or what I've learned in the talk, but I know in my heart that
something was changed in my mind. Something was envoked. I cannot tell
what it is right now, nor do I know how this would influence my future
careers. I can't even say that it is <strong>"useful"</strong>.</p>
<p>But as I am writing this, I remember David Brooks's talk about how
everyone has this part of them that does not care about fame or money or
tiktok likes, there is a part of us that pursues true happiness and
higher realm. I guess I begin to remember how I like Maths as Maths
itself, not because "Studying Maths Sounds So Hardcore" or "Maths Is
Useful for CS" or anything like that. And I guess that's what guest
lectures are for. To guide you to a higher perspective and envoke
interest. To put down the utilitarian thoughts and discover something
new. To truly enjoy the thing you love and remove the impurities
imparted by reality to those things.</p>
<p>I look forward to attending more guest lectures.</p>
<p><em>This lecture is not a Special one. I wouldn't say that the
lecture is the-thought-provoking-one. But as a first guest lecture that
I take the initiative to participate, and as a first guest lecture that
I attend in university, it does reminds me of something deep in my heart
and inspires me to attend future guest lectures.</em></p>
<h1 id="useless-notes">(Useless) Notes</h1>
<p>Here are the notes:(taken real quick so basically not useful lah)
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(k 2k) 1/(2k+1)(2k-1) </span><br><span class="line"></span><br><span class="line">AGM of Gauss: an = (an-1 +bn-1)/2, bn = sqrt(an-1 * bn-1)</span><br><span class="line">Kanada-Tamura-Yoshino=Ushiro</span><br><span class="line"></span><br><span class="line">Borwenis iteration</span><br><span class="line"></span><br><span class="line">Quadratic speed</span><br><span class="line"></span><br><span class="line">Ramanujan</span><br><span class="line">The man who knew infinity</span><br><span class="line">A Disappearing Number</span><br><span class="line"></span><br><span class="line">Chudnovsky series</span><br><span class="line"></span><br><span class="line">Ramanujan’s majic squares</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>Thoughts</category>
        <category>Experiences</category>
      </categories>
      <tags>
        <tag>Thoughts</tag>
        <tag>Mathematics</tag>
        <tag>Random</tag>
      </tags>
  </entry>
  <entry>
    <title>Machine Learning Introduction on Coursera</title>
    <url>/2024/02/19/Machine-Learning-Introduction-on-Coursera/</url>
    <content><![CDATA[<p>Took the <a
href="https://www.coursera.org/specializations/machine-learning-introduction">Machine
Learning Specialization</a> on Coursera, here's the notes.
<span id="more"></span></p>
<h1
id="supervised-machine-learning-regression-and-classification">Supervised
Machine Learning: Regression and Classification</h1>
<h2 id="week-1introduction-to-ml">Week 1:Introduction to ML</h2>
<h3 id="supervised-vs-unsupervised-learning">Supervised VS Unsupervised
Learning</h3>
<ol type="1">
<li>Supervised Learning</li>
</ol>
<ul>
<li><p>Input - Output prediction</p>
<ol type="a">
<li><p>Train your models with x and correct answers Y</p></li>
<li><p>Models take brand new X and predict Y</p></li>
</ol></li>
<li><p><strong>Regresion</strong> : predict X-Y mappings, where Y has
unlimited inifinte possible values</p></li>
<li><p><strong>CLassification</strong>: predict only a small number of
outputs Y (categories/classes)</p></li>
</ul>
<ol start="2" type="1">
<li>Unsupervised Learning</li>
</ol>
<ul>
<li>Find something interesting (structure) in unlabeled data. <strong>Do
not have an aim</strong>(e.g. <strong>clustering</strong>: (google news
cluster the related news togethe / grouping different customers);
<strong>Dimemsionality reduction</strong> ; <strong>Anomally
depression</strong>)</li>
</ul>
<h3 id="regression-model">Regression model</h3>
<h4 id="terminology">1. Terminology</h4>
<ul>
<li>Training Set: Data set used to train the model</li>
<li>x : Feature / input variable</li>
<li>y : "target" variable(the correct answer)</li>
<li>(x,y) : single training variable</li>
<li>(<span class="math inline">\(x^{(i)}\)</span>, <span
class="math inline">\(y^{(i)}\)</span>) : the ith row of training
example</li>
<li>m : number of training examples</li>
<li>f : hypothesis / model</li>
<li><span class="math inline">\(\hat{y}\)</span> : prediction</li>
<li><span class="math inline">\(f_{w,b}\)</span>(x) = wx + b <em>w, b:
coefficients / weight</em></li>
<li>Univariate Linear Regression: one variable</li>
</ul>
<h4 id="hands-on-pactice">2. Hands on pactice:</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line">plt.style.use(<span class="string">&#x27;./deeplearning.mplstyle&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># x_train is the input variable (size in 1000 square feet)</span></span><br><span class="line"><span class="comment"># y_train is the target (price in 1000s of dollars)</span></span><br><span class="line">x_train = np.array([<span class="number">1.0</span>, <span class="number">2.0</span>])</span><br><span class="line">y_train = np.array([<span class="number">300.0</span>, <span class="number">500.0</span>])</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;x_train = <span class="subst">&#123;x_train&#125;</span>&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;y_train = <span class="subst">&#123;y_train&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># m is the number of training examples</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;x_train.shape: <span class="subst">&#123;x_train.shape&#125;</span>&quot;</span>)</span><br><span class="line">m = x_train.shape[<span class="number">0</span>]</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;Number of training examples is: <span class="subst">&#123;m&#125;</span>&quot;</span>)</span><br><span class="line"><span class="comment"># m is the number of training examples</span></span><br><span class="line">m = <span class="built_in">len</span>(x_train)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;Number of training examples is: <span class="subst">&#123;m&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">i = <span class="number">0</span> <span class="comment"># Change this to 1 to see (x^1, y^1)</span></span><br><span class="line">x_i = x_train[i]</span><br><span class="line">y_i = y_train[i]</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;(x^(<span class="subst">&#123;i&#125;</span>), y^(<span class="subst">&#123;i&#125;</span>)) = (<span class="subst">&#123;x_i&#125;</span>, <span class="subst">&#123;y_i&#125;</span>)&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Plot the data points(marker : shape of the points, c : colour of points)</span></span><br><span class="line">plt.scatter(x_train, y_train, marker=<span class="string">&#x27;x&#x27;</span>, c=<span class="string">&#x27;r&#x27;</span>)</span><br><span class="line"><span class="comment"># Set the title</span></span><br><span class="line">plt.title(<span class="string">&quot;Housing Prices&quot;</span>)</span><br><span class="line"><span class="comment"># Set the y-axis label</span></span><br><span class="line">plt.ylabel(<span class="string">&#x27;Price (in 1000s of dollars)&#x27;</span>)</span><br><span class="line"><span class="comment"># Set the x-axis label</span></span><br><span class="line">plt.xlabel(<span class="string">&#x27;Size (1000 sqft)&#x27;</span>)</span><br><span class="line">plt.show()</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">compute_model_output</span>(<span class="params">x, w, b</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Computes the prediction of a linear model</span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">      x (ndarray (m,)): n-dimensional array of shape (m, ) </span></span><br><span class="line"><span class="string">      w,b (scalar)    : model parameters  </span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">      f_wb (ndarray (m,)): model prediction</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    m = x.shape[<span class="number">0</span>]</span><br><span class="line">    f_wb = np.zeros(m) <span class="comment"># one dimensional array with n entries</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(m):</span><br><span class="line">        f_wb[i] = w * x[i] + b</span><br><span class="line">        </span><br><span class="line">    <span class="keyword">return</span> f_wb</span><br><span class="line"></span><br><span class="line">tmp_f_wb = compute_model_output(x_train, w, b,)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Plot our model prediction</span></span><br><span class="line">plt.plot(x_train, tmp_f_wb, c=<span class="string">&#x27;b&#x27;</span>,label=<span class="string">&#x27;Our Prediction&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Plot the data points</span></span><br><span class="line">plt.scatter(x_train, y_train, marker=<span class="string">&#x27;x&#x27;</span>, c=<span class="string">&#x27;r&#x27;</span>,label=<span class="string">&#x27;Actual Values&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Set the title</span></span><br><span class="line">plt.title(<span class="string">&quot;Housing Prices&quot;</span>)</span><br><span class="line"><span class="comment"># Set the y-axis label</span></span><br><span class="line">plt.ylabel(<span class="string">&#x27;Price (in 1000s of dollars)&#x27;</span>)</span><br><span class="line"><span class="comment"># Set the x-axis label</span></span><br><span class="line">plt.xlabel(<span class="string">&#x27;Size (1000 sqft)&#x27;</span>)</span><br><span class="line">plt.legend() <span class="comment"># what does this do ?</span></span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<h4 id="cost-function">3. Cost Function</h4>
<ul>
<li>error: <span class="math inline">\(\hat{y^(i)}\)</span> - <span
class="math inline">\(y^(i)\)</span></li>
</ul>
<p><span class="math display">\[J(w,b) = \frac{1}{2m} \sum\limits_{i =
0}^{m-1} (f_{w,b}(x^{(i)}) - y^{(i)})^2 \tag{1}\]</span></p>
<p>[U] Why 1/2m ？</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">compute_cost</span>(<span class="params">x, y, w, b</span>): </span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Computes the cost function for linear regression.</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">      x (ndarray (m,)): Data, m examples </span></span><br><span class="line"><span class="string">      y (ndarray (m,)): target values</span></span><br><span class="line"><span class="string">      w,b (scalar)    : model parameters  </span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">        total_cost (float): The cost of using w,b as the parameters for linear regression</span></span><br><span class="line"><span class="string">               to fit the data points in x and y</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># number of training examples</span></span><br><span class="line">    m = x.shape[<span class="number">0</span>] </span><br><span class="line">    </span><br><span class="line">    cost_sum = <span class="number">0</span> </span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(m): </span><br><span class="line">        f_wb = w * x[i] + b   </span><br><span class="line">        cost = (f_wb - y[i]) ** <span class="number">2</span>  </span><br><span class="line">        cost_sum = cost_sum + cost  </span><br><span class="line">    total_cost = (<span class="number">1</span> / (<span class="number">2</span> * m)) * cost_sum  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> total_cost</span><br><span class="line"></span><br><span class="line">x_train = np.array([<span class="number">1.0</span>, <span class="number">1.7</span>, <span class="number">2.0</span>, <span class="number">2.5</span>, <span class="number">3.0</span>, <span class="number">3.2</span>])</span><br><span class="line">y_train = np.array([<span class="number">250</span>, <span class="number">300</span>, <span class="number">480</span>,  <span class="number">430</span>,   <span class="number">630</span>, <span class="number">730</span>,])</span><br><span class="line"></span><br><span class="line">fig, ax, dyn_items = plt_stationary(x_train, y_train)</span><br><span class="line">updater = plt_update_onclick(fig, ax, x_train, y_train, dyn_items)</span><br></pre></td></tr></table></figure>
<p>[U] Why is some places in the graph dense while others are loose?
Does that imply anything? <img src="CostFunction.png" /></p>
<h3 id="gradient-descent">Gradient Descent</h3>
<h4 id="implemetation">Implemetation</h4>
<p><span class="math display">\[\begin{align*} \text{repeat}&amp;\text{
until convergence:} \; \lbrace \newline
\;  w &amp;= w -  \alpha \frac{\partial J(w,b)}{\partial w} \tag{1}  \;
\newline
b &amp;= b -  \alpha \frac{\partial J(w,b)}{\partial b}  \newline
\rbrace
\end{align*}\]</span></p>
<p>gradient can be defined as:</p>
<p><span class="math display">\[
\begin{align}
\frac{\partial J(w,b)}{\partial w}  &amp;= \frac{1}{m} \sum\limits_{i =
0}^{m-1} (f_{w,b}(x^{(i)}) - y^{(i)})x^{(i)} \tag{2}\\
  \frac{\partial J(w,b)}{\partial b}  &amp;= \frac{1}{m} \sum\limits_{i
= 0}^{m-1} (f_{w,b}(x^{(i)}) - y^{(i)}) \tag{3}\\
\end{align}
\]</span></p>
<p><strong><span class="math inline">\(\alpha\)</span></strong> :
learning rate (determines how big a step would be) (always
<strong>positive</strong>)</p>
<blockquote>
<p>Change w by <strong>a little step</strong> to get
<strong>closer</strong> to the min.</p>
</blockquote>
<p>Repeat until <strong>convergence</strong> (w and b no longer changes
much)</p>
<p><em>That is to say:</em></p>
<p><em>if a is too small, gradient descent is slow;</em></p>
<p><em>if a is too large, gradient descent may overshoot(never reach
minimum), or fail to converge, or even diverge</em></p>
<p>If we use an appropriate fixed <span
class="math inline">\(\alpha\)</span> , then as we approach local
minimum, the steps become smaller because the derivative gets closer to
zero</p>
<p><strong>Simultaneous update</strong> :</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">tmp_w = ...</span><br><span class="line">tmp_b = ...</span><br><span class="line">w = tmp_w</span><br><span class="line">b = tmp_b</span><br></pre></td></tr></table></figure>
<ul>
<li>Why? Because we want the value of w and b to be the same when
calculating the derivative.</li>
</ul>
<p><strong>Batch Gradient descnet</strong>: for each step of the
gradient descent, we use all the training examples</p>
<h4 id="code">Code</h4>
<p>Functions to use:</p>
<ul>
<li><code>compute_gradient</code> implementing equation (2) and (3)
above</li>
<li><code>compute_cost</code> implementing <span
class="math inline">\(J(w,b)\)</span></li>
<li><code>gradient_descent</code>, utilizing compute_gradient and
compute_cost</li>
</ul>
<p><em>Code Convention: <span class="math inline">\(\frac{\partial
J(w,b)}{\partial b}\)</span> will be <code>dj_db</code>.</em></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">compute_gradient</span>(<span class="params">x, y, w, b</span>): </span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Computes the gradient for linear regression </span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">      x (ndarray (m,)): Data, m examples </span></span><br><span class="line"><span class="string">      y (ndarray (m,)): target values</span></span><br><span class="line"><span class="string">      w,b (scalar)    : model parameters  </span></span><br><span class="line"><span class="string">    Returns</span></span><br><span class="line"><span class="string">      dj_dw (scalar): The gradient of the cost w.r.t. the parameters w</span></span><br><span class="line"><span class="string">      dj_db (scalar): The gradient of the cost w.r.t. the parameter b     </span></span><br><span class="line"><span class="string">     &quot;&quot;&quot;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># Number of training examples</span></span><br><span class="line">    m = x.shape[<span class="number">0</span>]    </span><br><span class="line">    dj_dw = <span class="number">0</span></span><br><span class="line">    dj_db = <span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(m):  </span><br><span class="line">        f_wb = w * x[i] + b </span><br><span class="line">        dj_dw_i = (f_wb - y[i]) * x[i] </span><br><span class="line">        dj_db_i = f_wb - y[i] </span><br><span class="line">        dj_db += dj_db_i</span><br><span class="line">        dj_dw += dj_dw_i </span><br><span class="line">    dj_dw = dj_dw / m </span><br><span class="line">    dj_db = dj_db / m </span><br><span class="line">        </span><br><span class="line">    <span class="keyword">return</span> dj_dw, dj_db</span><br><span class="line"></span><br><span class="line">plt_gradients(x_train,y_train, compute_cost, compute_gradient)</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<p><strong>The 'quiver plot'</strong>: The arrow sizes reflect the
magnitude of the gradient at that point. The direction and slope of the
arrow reflects the ratio of ∂𝐽(𝑤,𝑏)∂𝑤 and ∂𝐽(𝑤,𝑏)∂𝑏</p>
<p>Now, we can implement Gradient Descent:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">gradient_descent</span>(<span class="params">x, y, w_in, b_in, alpha, num_iters, cost_function, gradient_function</span>): </span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Performs gradient descent to fit w,b. Updates w,b by taking </span></span><br><span class="line"><span class="string">    num_iters gradient steps with learning rate alpha</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">      x (ndarray (m,))  : Data, m examples </span></span><br><span class="line"><span class="string">      y (ndarray (m,))  : target val</span></span><br><span class="line"><span class="string">      ues</span></span><br><span class="line"><span class="string">      w_in,b_in (scalar): initial values of model parameters  </span></span><br><span class="line"><span class="string">      alpha (float):     Learning rate</span></span><br><span class="line"><span class="string">      num_iters (int):   number of iterations to run gradient descent</span></span><br><span class="line"><span class="string">      cost_function:     function to call to produce cost</span></span><br><span class="line"><span class="string">      gradient_function: function to call to produce gradient</span></span><br><span class="line"><span class="string">      </span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">      w (scalar): Updated value of parameter after running gradient descent</span></span><br><span class="line"><span class="string">      b (scalar): Updated value of parameter after running gradient descent</span></span><br><span class="line"><span class="string">      J_history (List): History of cost values</span></span><br><span class="line"><span class="string">      p_history (list): History of parameters [w,b] </span></span><br><span class="line"><span class="string">      &quot;&quot;&quot;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># An array to store cost J and w&#x27;s at each iteration primarily for graphing later</span></span><br><span class="line">    J_history = []</span><br><span class="line">    p_history = []</span><br><span class="line">    b = b_in</span><br><span class="line">    w = w_in</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(num_iters):</span><br><span class="line">        <span class="comment"># Calculate the gradient and update the parameters using gradient_function</span></span><br><span class="line">        dj_dw, dj_db = gradient_function(x, y, w , b)     </span><br><span class="line"></span><br><span class="line">        <span class="comment"># Update Parameters using equation (3) above</span></span><br><span class="line">        b = b - alpha * dj_db                            </span><br><span class="line">        w = w - alpha * dj_dw                            </span><br><span class="line"></span><br><span class="line">        <span class="comment"># Save cost J at each iteration</span></span><br><span class="line">        <span class="keyword">if</span> i&lt;<span class="number">100000</span>:      <span class="comment"># prevent resource exhaustion </span></span><br><span class="line">            J_history.append( cost_function(x, y, w , b))</span><br><span class="line">            p_history.append([w,b])</span><br><span class="line">        <span class="comment"># Print cost every at intervals 10 times or as many iterations if &lt; 10</span></span><br><span class="line">        <span class="keyword">if</span> i% math.ceil(num_iters/<span class="number">10</span>) == <span class="number">0</span>:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;Iteration <span class="subst">&#123;i:<span class="number">4</span>&#125;</span>: Cost <span class="subst">&#123;J_history[-<span class="number">1</span>]:<span class="number">0.2</span>e&#125;</span> &quot;</span>,</span><br><span class="line">                  <span class="string">f&quot;dj_dw: <span class="subst">&#123;dj_dw: <span class="number">0.3</span>e&#125;</span>, dj_db: <span class="subst">&#123;dj_db: <span class="number">0.3</span>e&#125;</span>  &quot;</span>,</span><br><span class="line">                  <span class="string">f&quot;w: <span class="subst">&#123;w: <span class="number">0.3</span>e&#125;</span>, b:<span class="subst">&#123;b: <span class="number">0.5</span>e&#125;</span>&quot;</span>) <span class="comment"># [U]what is this</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> w, b, J_history, p_history <span class="comment">#return w and J,w history for graphing</span></span><br></pre></td></tr></table></figure>
<p><img src="CostOverIterations.png" /></p>
<ul>
<li>The path makes steady (monotonic) progress toward its goal.</li>
<li>initial steps are much larger than the steps near the goal.</li>
</ul>
<h2 id="week-2-regression-with-multiple-input-variables">Week 2:
Regression with multiple input variables</h2>
<h3 id="multiple-linear-regression">Multiple Linear Regression</h3>
<h4 id="terminologies">Terminologies</h4>
<ol type="1">
<li><span class="math inline">\(x_{j}\)</span> : the jth feature</li>
<li>n: number of features</li>
<li><span class="math inline">\(\vec{x}^{(i)}\)</span> : feature of the
ith example (e.g.: <span class="math inline">\(\vec{x}^{(i)}\)</span> =
[1416 3 2 40])</li>
<li><span class="math inline">\(x_{j}^{(i)}\)</span> : value of the
feature j of the ith example. (e.g.: <span
class="math inline">\(x_{3}^{(2)}\)</span> = 2)</li>
</ol>
<p><span class="math display">\[f_{\vec{w}, b} ({\vec{x}}) = \vec{w} *
\vec{x} + b = w_{1}x_{1} + w_{2}x_{2} + ... + w_{n} x_{n} + b\]</span>
where <span class="math display">\[\vec{w} =
[w_{1}  w_{2}  w_{3}  ...  w_{n}]\]</span></p>
<h4 id="vectorization">Vectorization</h4>
<h5 id="arrays-in-python">Arrays in python</h5>
<p><em>In linnear algebra, count from 1; in np, we count from 0</em></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">f = np.dot(w, x) + b</span><br></pre></td></tr></table></figure>
<p>This <strong>vectorized implementation</strong> using parallel
hardware. It is more effecient than using for loop. ([U] Why?)</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">np.zeros(<span class="number">4</span>,) :  a = [<span class="number">0.</span> <span class="number">0.</span> <span class="number">0.</span> <span class="number">0.</span>], a shape = (<span class="number">4</span>,), a data <span class="built_in">type</span> = float64</span><br><span class="line">np.array([<span class="number">5</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">2</span>]):  a = [<span class="number">5</span> <span class="number">4</span> <span class="number">3</span> <span class="number">2</span>],     a shape = (<span class="number">4</span>,), a data <span class="built_in">type</span> = int64</span><br><span class="line">np.random.random_sample(<span class="number">4</span>): a = [<span class="number">0.97800919</span> <span class="number">0.13477195</span> <span class="number">0.04211348</span> <span class="number">0.20463884</span>], a shape = (<span class="number">4</span>,), a data <span class="built_in">type</span> = float64</span><br><span class="line">np.arange(<span class="number">4.</span>):     a = [<span class="number">0.</span> <span class="number">1.</span> <span class="number">2.</span> <span class="number">3.</span>], a shape = (<span class="number">4</span>,), a data <span class="built_in">type</span> = float64</span><br><span class="line"><span class="comment"># print(f&quot;np.arange(4.):     a = &#123;a&#125;, a shape = &#123;a.shape&#125;, a data type = &#123;a.dtype&#125;&quot;)</span></span><br></pre></td></tr></table></figure>
<h5 id="indexing-and-slicing">Indexing and Slicing</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Indexing</span></span><br><span class="line">a = np.arange(<span class="number">10</span>)</span><br><span class="line">a[<span class="number">2</span>].shape  <span class="comment"># 10</span></span><br><span class="line">a[-<span class="number">1</span>] <span class="comment"># 9</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Slicing: (start:stop:step) </span></span><br><span class="line">c = a[<span class="number">2</span>:<span class="number">7</span>:<span class="number">1</span>]; <span class="comment"># [2 3 4 5 6]</span></span><br><span class="line">c = a[<span class="number">2</span>:<span class="number">7</span>:<span class="number">2</span>]; <span class="comment"># [2 4 6]</span></span><br><span class="line">c = a[<span class="number">3</span>:] <span class="comment"># [3 4 5 6 7 8 9]</span></span><br><span class="line">c = a[:<span class="number">3</span>] <span class="comment"># [0 1 2]</span></span><br><span class="line">c = a[:]   <span class="comment"># return all elemtns</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Operations:</span></span><br><span class="line">a             : [<span class="number">1</span> <span class="number">2</span> <span class="number">3</span> <span class="number">4</span>]</span><br><span class="line">b = -a        : [-<span class="number">1</span> -<span class="number">2</span> -<span class="number">3</span> -<span class="number">4</span>]</span><br><span class="line">b = np.<span class="built_in">sum</span>(a) : <span class="number">10</span></span><br><span class="line">b = np.mean(a): <span class="number">2.5</span></span><br><span class="line">b = a**<span class="number">2</span>      : [ <span class="number">1</span>  <span class="number">4</span>  <span class="number">9</span> <span class="number">16</span>]</span><br></pre></td></tr></table></figure>
<h5 id="vector-operation">Vector Operation</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = np.array([ <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>])</span><br><span class="line">b = np.array([-<span class="number">1</span>,-<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>])</span><br><span class="line"></span><br><span class="line">a + b <span class="comment"># [0 0 6 8]</span></span><br><span class="line"><span class="comment"># operands could not be broadcast together with shapes (4,) (2,) </span></span><br><span class="line"></span><br><span class="line">b = <span class="number">5</span> * a <span class="comment"># [ 5 10 15 20]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Two dimensional array</span></span><br><span class="line">X = np.array([[<span class="number">1</span>],[<span class="number">2</span>],[<span class="number">3</span>],[<span class="number">4</span>]])</span><br></pre></td></tr></table></figure>
<h5 id="two-dimensional-arrays">Two Dimensional Arrays</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = np.zeros((<span class="number">1</span>, <span class="number">5</span>)) <span class="comment"># a = [[0. 0. 0. 0. 0.]]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Reshape</span></span><br><span class="line">a = np.arange(<span class="number">6</span>).reshape(<span class="number">2</span>, -<span class="number">1</span>) <span class="comment"># -1 means make the computer decide</span></span><br><span class="line"><span class="comment">#a= [[0 1 2] [3 4 5]]</span></span><br><span class="line">a = numpy.array([[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>], [<span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>]])</span><br><span class="line">a.reshape(-<span class="number">1</span>)</span><br><span class="line">array([[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>]])</span><br><span class="line"></span><br><span class="line"><span class="comment"># Index</span></span><br><span class="line">a[<span class="number">2</span>, <span class="number">0</span>] <span class="comment"># row 2, column 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Slicing</span></span><br><span class="line">a = </span><br><span class="line">[[ <span class="number">0</span>  <span class="number">1</span>  <span class="number">2</span>  <span class="number">3</span>  <span class="number">4</span>  <span class="number">5</span>  <span class="number">6</span>  <span class="number">7</span>  <span class="number">8</span>  <span class="number">9</span>]</span><br><span class="line"> [<span class="number">10</span> <span class="number">11</span> <span class="number">12</span> <span class="number">13</span> <span class="number">14</span> <span class="number">15</span> <span class="number">16</span> <span class="number">17</span> <span class="number">18</span> <span class="number">19</span>]]</span><br><span class="line"></span><br><span class="line">a[<span class="number">0</span>, <span class="number">2</span>:<span class="number">7</span>:<span class="number">1</span>] =  [<span class="number">2</span> <span class="number">3</span> <span class="number">4</span> <span class="number">5</span> <span class="number">6</span>] </span><br><span class="line"></span><br><span class="line">a[:, <span class="number">2</span>:<span class="number">7</span>:<span class="number">1</span>] = </span><br><span class="line"> [[ <span class="number">2</span>  <span class="number">3</span>  <span class="number">4</span>  <span class="number">5</span>  <span class="number">6</span>]</span><br><span class="line"> [<span class="number">12</span> <span class="number">13</span> <span class="number">14</span> <span class="number">15</span> <span class="number">16</span>]] </span><br><span class="line"></span><br><span class="line">a[:,:].shape = (<span class="number">2</span>, <span class="number">10</span>)</span><br><span class="line">a[<span class="number">1</span>,:] =  [<span class="number">10</span> <span class="number">11</span> <span class="number">12</span> <span class="number">13</span> <span class="number">14</span> <span class="number">15</span> <span class="number">16</span> <span class="number">17</span> <span class="number">18</span> <span class="number">19</span>] </span><br><span class="line">a[<span class="number">1</span>]   =  [<span class="number">10</span> <span class="number">11</span> <span class="number">12</span> <span class="number">13</span> <span class="number">14</span> <span class="number">15</span> <span class="number">16</span> <span class="number">17</span> <span class="number">18</span> <span class="number">19</span>] </span><br></pre></td></tr></table></figure>
<h4 id="implementation">Implementation:</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">compute_cost</span>(<span class="params">X, y, w, b</span>): </span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    compute cost</span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">      X (ndarray (m,n)): Data, m examples with n features</span></span><br><span class="line"><span class="string">      y (ndarray (m,)) : target values</span></span><br><span class="line"><span class="string">      w (ndarray (n,)) : model parameters  </span></span><br><span class="line"><span class="string">      b (scalar)       : model parameter</span></span><br><span class="line"><span class="string">      </span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">      cost (scalar): cost</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">compute_gradient</span>(<span class="params">X, y, w, b</span>): </span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Computes the gradient for linear regression </span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">      X (ndarray (m,n)): Data, m examples with n features</span></span><br><span class="line"><span class="string">      y (ndarray (m,)) : target values</span></span><br><span class="line"><span class="string">      w (ndarray (n,)) : model parameters  </span></span><br><span class="line"><span class="string">      b (scalar)       : model parameter</span></span><br><span class="line"><span class="string">      </span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">      dj_dw (ndarray (n,)): The gradient of the cost w.r.t. the parameters w. </span></span><br><span class="line"><span class="string">      dj_db (scalar):       The gradient of the cost w.r.t. the parameter b. </span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line"> <span class="comment"># do remember to simultaneously calculate the gradients</span></span><br><span class="line"> <span class="keyword">def</span> <span class="title function_">gradient_descent</span>(<span class="params">X, y, w_in, b_in, cost_function, gradient_function, alpha, num_iters</span>): </span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Performs batch gradient descent to learn w and b. Updates w and b by taking </span></span><br><span class="line"><span class="string">    num_iters gradient steps with learning rate alpha</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">      X (ndarray (m,n))   : Data, m examples with n features</span></span><br><span class="line"><span class="string">      y (ndarray (m,))    : target values</span></span><br><span class="line"><span class="string">      w_in (ndarray (n,)) : initial model parameters  </span></span><br><span class="line"><span class="string">      b_in (scalar)       : initial model parameter</span></span><br><span class="line"><span class="string">      cost_function       : function to compute cost</span></span><br><span class="line"><span class="string">      gradient_function   : function to compute the gradient</span></span><br><span class="line"><span class="string">      alpha (float)       : Learning rate</span></span><br><span class="line"><span class="string">      num_iters (int)     : number of iterations to run gradient descent</span></span><br><span class="line"><span class="string">      </span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">      w (ndarray (n,)) : Updated values of parameters </span></span><br><span class="line"><span class="string">      b (scalar)       : Updated value of parameter </span></span><br><span class="line"><span class="string">      &quot;&quot;&quot;</span></span><br><span class="line">  <span class="comment"># An array to store cost J and w&#x27;s at each iteration primarily for graphing later</span></span><br><span class="line">    J_history = []</span><br><span class="line">    w = copy.deepcopy(w_in)  <span class="comment">#avoid modifying global w within function</span></span><br><span class="line">    b = b_in</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(num_iters):</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Calculate the gradient and update the parameters</span></span><br><span class="line">        dj_db,dj_dw = gradient_function(X, y, w, b)   <span class="comment">##None</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># Update Parameters using w, b, alpha and gradient</span></span><br><span class="line">        w = w - alpha * dj_dw               <span class="comment">##None</span></span><br><span class="line">        b = b - alpha * dj_db               <span class="comment">##None</span></span><br><span class="line">      </span><br><span class="line">        <span class="comment"># Save cost J at each iteration</span></span><br><span class="line">        <span class="keyword">if</span> i&lt;<span class="number">100000</span>:      <span class="comment"># prevent resource exhaustion </span></span><br><span class="line">            J_history.append( cost_function(X, y, w, b))</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Print cost every at intervals 10 times or as many iterations if &lt; 10</span></span><br><span class="line">        <span class="keyword">if</span> i% math.ceil(num_iters / <span class="number">10</span>) == <span class="number">0</span>:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;Iteration <span class="subst">&#123;i:4d&#125;</span>: Cost <span class="subst">&#123;J_history[-<span class="number">1</span>]:<span class="number">8.2</span>f&#125;</span>   &quot;</span>)</span><br><span class="line">        </span><br><span class="line">    <span class="keyword">return</span> w, b, J_history <span class="comment">#return final w,b and J history for graphing</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># plot cost versus iteration  </span></span><br><span class="line">fig, (ax1, ax2) = plt.subplots(<span class="number">1</span>, <span class="number">2</span>, constrained_layout=<span class="literal">True</span>, figsize=(<span class="number">12</span>, <span class="number">4</span>))</span><br><span class="line">ax1.plot(J_hist) <span class="comment"># calculated Cost history</span></span><br><span class="line">ax2.plot(<span class="number">100</span> + np.arange(<span class="built_in">len</span>(J_hist[<span class="number">100</span>:])), J_hist[<span class="number">100</span>:])</span><br><span class="line">ax1.set_title(<span class="string">&quot;Cost vs. iteration&quot;</span>);  ax2.set_title(<span class="string">&quot;Cost vs. iteration (tail)&quot;</span>)</span><br><span class="line">ax1.set_ylabel(<span class="string">&#x27;Cost&#x27;</span>)             ;  ax2.set_ylabel(<span class="string">&#x27;Cost&#x27;</span>) </span><br><span class="line">ax1.set_xlabel(<span class="string">&#x27;iteration step&#x27;</span>)   ;  ax2.set_xlabel(<span class="string">&#x27;iteration step&#x27;</span>) </span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<h4 id="normal-equation">Normal Equation</h4>
<ul>
<li>Only for linear regression</li>
<li>Solve for w,b without iteration</li>
</ul>
<p>[U] wut is this?</p>
<p><span class="math display">\[
\begin{align}
\frac{\partial J(\mathbf{w},b)}{\partial w_j}  &amp;= \frac{1}{m}
\sum\limits_{i = 0}^{m-1} (f_{\mathbf{w},b}(\mathbf{x}^{(i)}) -
y^{(i)})x_{j}^{(i)} \tag{6}  \\
\frac{\partial J(\mathbf{w},b)}{\partial b}  &amp;= \frac{1}{m}
\sum\limits_{i = 0}^{m-1} (f_{\mathbf{w},b}(\mathbf{x}^{(i)}) - y^{(i)})
\tag{7}
\end{align}
\]</span></p>
<h3 id="gradient-descent-in-practice">Gradient Descent in Practice</h3>
<h4 id="feature-scaling">Feature scaling</h4>
<p>When the possible values of a feature is large, then the initial w
choose would be relatively small. A small change of w might have a huge
impact on the cost.</p>
<p>When the value of the features very widely, then it's very likely
that the gradient descent is inefficient (similar to the situation when
learning rage <span class="math inline">\(\alpha\)</span> is too
large.)</p>
<p>To solve this, we do <strong>rescaling</strong>.</p>
<p>In rescaling, we aim for -1 &lt;= <span
class="math inline">\(x_j\)</span> &lt;= 1</p>
<p><strong>Example</strong></p>
<p>300&lt;= <span class="math inline">\(x_1\)</span> &lt;= 2000 , 0
&lt;= <span class="math inline">\(x_2\)</span> &lt;= 5</p>
<ol type="1">
<li><p>Dividing by maximum</p>
<p>rescale 0.15 &lt;= <span class="math inline">\(x_1\)</span> &lt;= 1 ,
0 &lt;= <span class="math inline">\(x_1\)</span> &lt;=5</p></li>
<li><p>Mean Normalization</p>
<p>rescale x to -1 &lt;= x &lt;= 1</p>
<p><span class="math display">\[x_i := \dfrac{x_i - \mu_i}{max - min}
\]</span></p>
<p>where <span class="math inline">\(\mu\)</span> is the
average</p></li>
<li><p>Z-score Normalization</p>
<p>rescale x to have a mean 0 and standard deviation of 1([U] How?)</p>
<p><span class="math display">\[x^{(i)}_j = \dfrac{x^{(i)}_j -
\mu_j}{\sigma_j} \]</span></p></li>
</ol>
<h4 id="checking-the-convergence-of-gradient-descent">Checking the
Convergence of Gradient Descent</h4>
<p><strong>Learning Curve</strong></p>
<p><img src="LearningCurve.png" /></p>
<p><strong>Automatic Convergence Test</strong></p>
<p>Let <span class="math inline">\(\epsilon\)</span> be ..., if J(w,b)
decreases by &lt;= <span class="math inline">\(\epsilon\)</span> on one
iteration, declare convergence</p>
<p><strong>Identifying the Problem</strong></p>
<figure>
<img src="LearningRateTooLarge.png" style="width:60.0%"
alt="Learning Rate Too Large" />
<figcaption aria-hidden="true">Learning Rate Too Large</figcaption>
</figure>
<p><em>Tips: if the Gradient Descent is not working, change <span
class="math inline">\(\alpha\)</span> to a very small number and check
if it is decreasing, else there's something wrong with the code</em></p>
<p><em>Values of <span class="math inline">\(\alpha\)</span> to choose :
0.01 - 0.03 - 0.1 - 0.3 - 1 - 3 ...</em></p>
<h1 id="useful-referenceswebsites">Useful References/Websites:</h1>
<ol type="1">
<li>https://numpy.org/doc/stable/</li>
<li>https://numpy.org/doc/stable/user/basics.broadcasting.html</li>
</ol>
<p>[ ] Todo: should learn python</p>
<p>[ ] Todo: clean the code above</p>
<p>TBC.</p>
]]></content>
      <categories>
        <category>Notes</category>
        <category>CS</category>
        <category>AI</category>
        <category>MachineLearning</category>
      </categories>
      <tags>
        <tag>ComputerScience</tag>
        <tag>Notes</tag>
        <tag>Study</tag>
        <tag>Coursera</tag>
        <tag>Stanford</tag>
        <tag>AI</tag>
        <tag>MachineLearning</tag>
        <tag>SelfStudy</tag>
        <tag>OnlineCourses</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>吹灭读书灯，一身都是月</title>
    <url>/2024/02/17/%E5%90%B9%E7%81%AD%E8%AF%BB%E4%B9%A6%E7%81%AF%EF%BC%8C%E4%B8%80%E8%BA%AB%E9%83%BD%E6%98%AF%E6%9C%88/</url>
    <content><![CDATA[<p>吹灭读书灯，一身都是月，多美的句子啊。</p>
<span id="more"></span>
<p>小的时候最喜欢的房子就是：</p>
<p>小小的房间，与世人的喧嚣隔绝。屋子里的东西不多，一张朴素的床，一个放得满满的书架，一个能在晚上眺望星空的窗户，一张书桌。
比如曾经看过一本漫画书（忘记叫什么名字了），讲一个老爷爷和他的小精灵生活在一个农场里。那个小精灵的房子在田地的中央，每天晚上他就爬上去，打开昏黄的台灯，就着星光读书。</p>
<p>又比如里约大冒险里那个小男孩，住在深巷中的一个房子的天台上，甚至不能叫屋子，只是雨棚下的简单铺成的床。每天晚上，他听着巷子里的人声喧闹，爬着水管回到家中，从破烂的箱子里拿出一杯饮料，坐下来看星星看月亮。</p>
<p>而大学一学期的时光里，我最享受的时刻就是上完了课去完了cca回到宿舍里，洗完澡之后坐下来，煮一碗泡面，看着窗外的星光，伏案学习。这样的时光里，我感受不到时间的流逝。人生的享受不过如此。</p>
<p>在上大学之前我从来没住过宿，也从来没有远离过家乡，有时候一个人呆着难免会觉得孤独。当我试图通过互联网转移注意力、试图在无意义的社交中浪费时间，孤独感并不会得到缓解。唯有在这种夜深人静的时刻，吹灭读书灯一身都是月的时候，我才能清清楚楚明明白白地感受到生命的存在，感受到意义，消解孤独。</p>
<p>突然又想到一句话，好像和以上没什么联系：人的一生，苦也罢，乐也罢，的也罢，失也罢，要紧的是心中要有一泓清泉不能没有月辉。</p>
<p><img src="dorm.jpg" /></p>
]]></content>
      <categories>
        <category>Thoughts</category>
      </categories>
      <tags>
        <tag>Study</tag>
        <tag>Thoughts</tag>
        <tag>Random</tag>
        <tag>Peace</tag>
      </tags>
  </entry>
  <entry>
    <title>General Studying Tips</title>
    <url>/2024/02/11/General-Studying-Tips/</url>
    <content><![CDATA[<p>General study tips.</p>
<span id="more"></span>
<ol type="1">
<li>Use Red pen for knowledge that I don't remember when looking back;
Blue pen for additional knowledge</li>
</ol>
]]></content>
      <categories>
        <category>Notes</category>
        <category>General</category>
      </categories>
      <tags>
        <tag>Notes</tag>
        <tag>Study</tag>
        <tag>StudyTips</tag>
        <tag>General</tag>
      </tags>
  </entry>
  <entry>
    <title>我的世界起床战争技巧总结</title>
    <url>/2024/02/11/%E6%88%91%E7%9A%84%E4%B8%96%E7%95%8C%E8%B5%B7%E5%BA%8A%E6%88%98%E4%BA%89%E6%8A%80%E5%B7%A7%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>我的世界起床战争技巧总结 <span id="more"></span></p>
<h1 id="pvp">PVP</h1>
<h2 id="控距">控距</h2>
<p>将敌人保持在自己攻击范围的边上。为了保持这一点，手可以时不时按<code>w</code>而不是一直按着键盘</p>
<h2 id="攻击">攻击</h2>
<p>跳劈可以用，但是容易翻车。可以尝试在第一次攻击的时候跳劈，注意往旁边跳劈会比较好一点。</p>
<h1 id="搭路">搭路</h1>
<p>找到节奏感</p>
]]></content>
      <categories>
        <category>Games</category>
        <category>Minecraft</category>
        <category>Bedwars</category>
      </categories>
      <tags>
        <tag>Games</tag>
        <tag>Minecraft</tag>
        <tag>Entertainment</tag>
        <tag>Fun</tag>
        <tag>Skills</tag>
        <tag>Bedwars</tag>
      </tags>
  </entry>
  <entry>
    <title>Notes for CS2100</title>
    <url>/2024/02/06/Notes-for-CS2100/</url>
    <content><![CDATA[<p>Notes for CS2100 - Computer Organization</p>
<span id="more"></span>
<h1 id="c-programming">C Programming</h1>
<h2 id="introduction-to-c">Introduction to C</h2>
<h3 id="features-of-c">Features of C</h3>
<ol type="1">
<li><p><strong>Computer organisation</strong> is the study of internal
working structuring and implementation of a computer system. It refers
to the level of abstraction above the digital logic level, but below the
operating system level.</p></li>
<li><p>Why is C <strong>so fast</strong>?</p>
<p>Edit code(vim tmp.c) -&gt; <strong>Source code(tmp.c)</strong> -&gt;
Compile(gcc tmp.out) -&gt; Executable Code(tmp.out) -&gt;
Output</p></li>
<li><p><strong>von Neumann architecture</strong>(Prinston
Architecture)</p>
<ul>
<li><p>Central Processing Unit</p>
<ul>
<li>registers</li>
<li>A control unit containing an instruction register and program
counter</li>
<li>Arithmetic/Logic Unit(ALU)</li>
</ul></li>
<li><p>Memory Random Access Memory</p></li>
<li><p>Input Output Devices (I/O Devices)</p></li>
</ul></li>
<li><p>Why every varaible <strong>must be declared</strong>?</p>
<p>The execution code for different variables in Source Code is
different</p></li>
<li><p>Data Types</p>
<ul>
<li>int (<span class="math inline">\(2^{31}\)</span> To <span
class="math inline">\(-2^{31}\)</span>) 4 bytes</li>
<li>float(4 bytes) or double(8 byte)</li>
<li>chars(8 bit integer)</li>
</ul>
<p><code>sizeof( &lt;variable&gt; or &lt;DataType&gt; )</code> : returns
the size of the variable</p></li>
</ol>
<h3 id="sample-c-programme">Sample C Programme</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*Preprocessor Directives*/</span></span><br><span class="line"><span class="comment">//Header Files</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="comment">//Macro Expansions (compiler substitudes Pi to 3.14 when compiling.</span></span><br><span class="line"><span class="comment">//                  If #define PI 3.14; then PI is substituded to 3.14;)</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PI 3.14 <span class="comment">// please use full CAP</span></span></span><br><span class="line"><span class="comment">//Conditional Compilation([U]: wuts this)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Main Function: Input/Output */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123; <span class="comment">// will always return 0 if executed correctly</span></span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">double</span> cap;</span><br><span class="line">    <span class="comment">//printf(format string);</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;What is your age?&quot;</span>);</span><br><span class="line">    <span class="comment">//scanf(format string, input list); </span></span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;age); <span class="comment">// &amp;age: the address of the variable </span></span><br><span class="line">    <span class="comment">// %lf double - %e float or double</span></span><br><span class="line">    <span class="comment">//%5d: display integer in width of 5, right justified(123456 - 23456)</span></span><br><span class="line">    <span class="comment">//%8.3f: a is minimum number of characters</span></span><br><span class="line">    <span class="comment">//       including whole part, point and decimal part</span></span><br><span class="line">    <span class="comment">//       b number of characters of decimal part</span></span><br><span class="line">    <span class="comment">//       If total number of characters are less than a</span></span><br><span class="line">    <span class="comment">//       it fills with blank spaces at the left side of number</span></span><br><span class="line"></span><br><span class="line">With - sign it fills blank spaces at the right side of the number</span><br><span class="line">    <span class="comment">//escape sequence: \n \t(tab) \&quot;(double quote) %% percent</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">-Wall <span class="comment">//gives all warnings</span></span><br><span class="line">-o &lt;Name&gt;<span class="comment">// specifies the name of executable file(default a.out)</span></span><br><span class="line">-lm <span class="comment">//needed for mathematical functions headers like math.h</span></span><br></pre></td></tr></table></figure>
<h2 id="computation">Computation</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* */</span></span><br><span class="line"><span class="comment">//letters, numbers and _ can be used. but should not satrt with numbers</span></span><br><span class="line"><span class="comment">//Avoid standard identifiers(!= reserved words; does not cause warning) </span></span><br><span class="line"><span class="type">int</span> count, Count; </span><br><span class="line"></span><br><span class="line"><span class="comment">/*Assignment*/</span></span><br><span class="line"><span class="comment">//a: lvalue</span></span><br><span class="line">a = <span class="number">32</span>;</span><br><span class="line"><span class="comment">// Cascading. assigns from right to left. </span></span><br><span class="line">a = b = c = <span class="number">3</span> + <span class="number">6</span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">/*Operations*/</span></span><br><span class="line"><span class="comment">//binary Operations: Left Associative</span></span><br><span class="line"><span class="comment">//[U] (int) / (double) / (int) or (double) / (int) / (int), </span></span><br><span class="line"><span class="comment">//if the value doesn&#x27;t change but the type changes, would the answer be different?</span></span><br><span class="line"><span class="comment">//Or when is the case they are different?</span></span><br><span class="line"><span class="number">46</span> / <span class="number">15</span> / <span class="number">2</span> = <span class="number">3</span> / <span class="number">2</span> = <span class="number">1</span></span><br><span class="line"><span class="comment">//Unary Operators: + - are right associative</span></span><br><span class="line">a = <span class="number">-23</span>;</span><br><span class="line"><span class="comment">//Truncate result</span></span><br><span class="line"><span class="type">int</span> n = <span class="number">9</span> * <span class="number">0.5</span>; <span class="comment">// n = 4</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*Precedence*/</span></span><br><span class="line"><span class="comment">//()    exp++   exp--</span></span><br><span class="line"><span class="comment">//* &amp; + - (unary) ++expr --expr typecast</span></span><br><span class="line"><span class="comment">//(* / % ) &gt; (+ -) &gt; (&gt; &lt;) &gt; (== !=) &gt; (&amp;&amp;) &gt; (||)</span></span><br><span class="line"><span class="comment">// = += -= *=...</span></span><br><span class="line">x = (<span class="literal">true</span> || <span class="literal">false</span> &amp;&amp; <span class="literal">true</span>) <span class="comment">//This evaluates to true because &amp;&amp; is executed first</span></span><br><span class="line">b = a++; <span class="comment">// assign a to b then increment</span></span><br><span class="line">b = ++a; <span class="comment">// a ++ and then b = a</span></span><br><span class="line"><span class="comment">//Mixed-Type Arithmetic Operation</span></span><br><span class="line"><span class="type">float</span> p = <span class="number">10</span> / <span class="number">4</span>; <span class="comment">//p = 2.0</span></span><br><span class="line"><span class="type">int</span> n = <span class="number">10</span> / <span class="number">4.0</span>; <span class="comment">//n = 2.5 = 2;</span></span><br><span class="line"><span class="type">int</span> r = <span class="number">-10</span> / <span class="number">4.0</span>; <span class="comment">// r = -2; </span></span><br><span class="line"><span class="type">int</span> aa = <span class="number">6</span>;</span><br><span class="line"><span class="type">float</span> pp = (<span class="type">float</span>) aa / <span class="number">4</span>; <span class="comment">// pp = 1.5</span></span><br><span class="line"><span class="type">float</span> qq = (<span class="type">float</span>) (aa / <span class="number">4</span>) <span class="comment">// qq = 1.0</span></span><br><span class="line"><span class="comment">//modulo and remainder</span></span><br><span class="line">a = <span class="number">-10</span>%<span class="number">4</span> <span class="comment">// a = -2 this is a remainder</span></span><br><span class="line">          <span class="comment">//in python this is a modulo, a = 2;</span></span><br></pre></td></tr></table></figure>
<h2 id="selection-structures">Selection Structures</h2>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*Selection Strcture*/</span></span><br><span class="line"><span class="comment">// Notice the break; here. If no break is put here, then if the value == value2</span></span><br><span class="line"><span class="comment">// Then the programme will execute Code1 &amp; Code 2 &amp; default</span></span><br><span class="line"><span class="keyword">switch</span>( &lt;variable or expression&gt; ) &#123;</span><br><span class="line">    <span class="keyword">case</span> value1:</span><br><span class="line">        Cod1 to execute</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> value2:</span><br><span class="line">        Code2 to execute</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        Code to execute <span class="keyword">if</span> no <span class="keyword">case</span> is matched</span><br><span class="line">        <span class="keyword">break</span>; </span><br><span class="line"><span class="comment">//lazy evaluation || short circuit evaluation</span></span><br><span class="line"><span class="keyword">if</span>(<span class="literal">true</span> || (runtime error statement)) <span class="comment">// return : true.</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="number-system">Number System</h1>
<h2 id="introduction">Introduction</h2>
<p><strong>Decimal Number System</strong> 1. Weighted-posistional number
system 2. Base(radix) is 10 3. Each position has a weight of power of
10. (1.2 where 2 has a weight of <span
class="math inline">\(2^{-1}\)</span>) 4. e.g.: 7594.36 is a 6 digit
number with 2 decimal places</p>
<p><strong>Prefix</strong> 1. 0 for octal(8) 2. 0x for hexadecimal</p>
<p><strong>Converting Decimals</strong></p>
<figure>
<img src="Carries.png" alt="Converting Decimals" />
<figcaption aria-hidden="true">Converting Decimals</figcaption>
</figure>
<h2 id="american-standard-code-for-information-interchange">American
Standard Code for Information Interchange</h2>
<ul>
<li><p>7 bits, plus 1 parity bit(odd or even parity) (Count the number
of 1 bits and check whether it is even/odd(_0101101: odd parity should
be 0))</p></li>
<li><p>Converting: '9' - '0' = 9</p></li>
</ul>
<h2 id="signed-and-unsigned-values">Signed and Unsigned Values</h2>
<h3 id="ways-to-represent">Ways to represent</h3>
<ol type="1">
<li><p><strong>Sign-and-Magnitude</strong></p>
<ul>
<li>sign bit: 0 for + and 1 for -</li>
<li><span class="math inline">\({10100000}_{sm} = 33_{10}\)</span></li>
</ul></li>
<li><p><strong>1s complment</strong></p>
<ul>
<li>-x = <span class="math inline">\(2^{n}\)</span> - x - 1</li>
<li>to negate x, just invert all the bits</li>
<li>MSB still represents the sign, 0 for positive, 1 for negative</li>
<li>MSB's weight: (in n bits, ) <span class="math inline">\(-2^{n-1} +
1\)</span></li>
</ul></li>
<li><p><strong>2s-complement</strong></p>
<ul>
<li>-x = <span class="math inline">\(2^n\)</span> - x</li>
<li>Negate x: invert all bits and add 1</li>
<li>shortcut: from right to left, copy everything until the first 1.
invert everything to the left of the first 1. (e.g: 00101000 -&gt;
11011000)</li>
<li>MSB still represent the sign, 0 for positive</li>
<li><strong>in a 4-bit 2s number, the MSB has a weight of -8 ([U] Prove
that?)</strong></li>
</ul></li>
<li><p><strong>Excess Notation</strong></p>
<ul>
<li>Allows the range of values to be split evenly betweent the positive
and negative values.</li>
<li>the Excess-n notation means that in order for a negative number x to
be negated, you need to add n to it. (e.g.: for the excess-4 notation,
add 4 to -2 inorder to get 2) <img src="Excess4.png"
alt="Excess-4 representation on 3-bit numbers" /></li>
</ul></li>
</ol>
<p>For a 4-bit system, - SM: 0 to 7, -0 to -7 - 1s: 0 to 7, -0 to -7 -
2s: 0 to 7, -1 to -8 <em>-8 is 1000</em></p>
<p>So <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">2147483647</span>; <span class="comment">// 2^31 - 1</span></span><br><span class="line">x = x + <span class="number">1</span>;<span class="comment">// 0111...1111 + 1 = 1000....000 = (-2147483648)10</span></span><br></pre></td></tr></table></figure></p>
<p><strong>Complement on Fractions</strong> For fractions, simply ignore
the '.'</p>
<h2 id="addition-and-subtraction">Addition and Subtraction</h2>
<h3 id="s-complement">2s Complement</h3>
<p>Simply disgard the additional bit <a
href="Why%20does%20this%20work?">U</a></p>
<p>For subtraction, add <span class="math inline">\((-x)_{2s}\)</span>
<a href="Why%20does%20this%20work?">U</a></p>
<p><strong>Overflow Checking</strong></p>
<pre><code>If the carry in and carry out of the MSB is different, then a overflow occurs.

e.g.: 1001 + 1101 - in:0 out: 1 thus an overflow</code></pre>
<h3 id="s-complement-1">1s Complement</h3>
<ol type="1">
<li>Addition: If there is a carry out of the MSB, add 1 to the result
([U] why does this work?)</li>
<li>Subtraction: add <span class="math inline">\((-x)_{1s}\)</span></li>
</ol>
<h2 id="representation-of-numbers">Representation of Numbers</h2>
<h3 id="fixed-vs-floating">Fixed vs Floating</h3>
<ol type="1">
<li><p>Fixed Point Representation The allocated number for the number
part and fractional part are fixed.</p></li>
<li><p>Floating Point Representation Allows representation of very large
and very small numbers.</p></li>
</ol>
<h3 id="ieee-754-floating-point-rep.">IEEE 754 Floating-Point Rep.</h3>
<ol type="1">
<li>Components:<strong>sign - exponent -
mantissa(fraction)</strong></li>
</ol>
<p><em>The sign is for the mantissa, not the exponent</em></p>
<ol start="2" type="1">
<li>Format: <strong>Single-precision(32 bits)</strong>
<ul>
<li><p>1-bit sign, 8-bit exponent with bias127(excess-127), 23-bit
mantissa</p></li>
<li><p>The Mantissa is normalised with an implicit leading bit 1.</p>
<p>This means that we will always keep the part to the left of the point
1. Only the left part will be stored. e.g.: (110.1<span
class="math inline">\()_2\)</span> = 1.10<span
class="math inline">\(1_2\)</span> * <span
class="math inline">\(2^2\)</span>, only 101 is stored in the mantissa.
<em>Normalisation: 123.2 </em> <span
class="math inline">\(10^{0}\)</span> = 1.232 * <span
class="math inline">\(10^{2}\)</span>*</p></li>
</ul></li>
<li>E.g.: To find the IEEE 754 representation of -6.5 <span
class="math display">\[-6.5_{10} = -110.1_{2} = -1.101_{2} *
2^{2}\]</span> Since <span class="math display">\[Exponent = 2 + 127 =
129 = 10000001_2\]</span> <span class="math display">\[ (-110.1)_2 =
1_{sign}10000001_{exponent}{101 0000 ... 0000}_{mantissa}\]</span> Or in
hexadecimal <span class="math display">\[1 10000001 1010000...00000_2 =
C0D00000_{16}\]</span></li>
</ol>
<h1 id="things-to-say">Things to say</h1>
<p>Why do we even compare C with Python in this course?</p>
]]></content>
      <categories>
        <category>Notes</category>
        <category>CS</category>
        <category>ComputerOrganization</category>
      </categories>
      <tags>
        <tag>ComputerScience</tag>
        <tag>Notes</tag>
        <tag>CPP</tag>
        <tag>C</tag>
        <tag>MIPS</tag>
        <tag>OperatingSystem</tag>
      </tags>
  </entry>
  <entry>
    <title>Thought - 2024.2</title>
    <url>/2024/02/03/Thought-2024-2/</url>
    <content><![CDATA[<p>Thougts in Feb 2024</p>
<span id="more"></span>
<blockquote>
<p>2.1 : opportunities will come!</p>
</blockquote>
<blockquote>
<p>2.2 : Tomorrow's a brand new day!</p>
</blockquote>
<blockquote>
<p>2.3 : Everything will be all right.</p>
</blockquote>
<blockquote>
<p>2.4 : Focus, even if it's one tiny thing.</p>
</blockquote>
<blockquote>
<p>2.5 : 戒骄戒躁。</p>
</blockquote>
<blockquote>
<p>2.6 : Keep Going</p>
</blockquote>
<blockquote>
<p>2.10 : 哎呀好像断更了。祝大家新年快乐！</p>
</blockquote>
<blockquote>
<p>2.12 : Try new things!</p>
</blockquote>
<blockquote>
<p>2.13 : Deal with loneliness</p>
</blockquote>
<blockquote>
<p>2.14 : 拓宽人脉！</p>
</blockquote>
<blockquote>
<p>2.18 : Making progress!</p>
</blockquote>
<blockquote>
<p>2.21 : <a
href="https://wfyishere.github.io/2024/02/20/Thoughts-for-Attending-Guest-Lecture-on-Pi/">guest
lecture is fun.</a></p>
</blockquote>
<blockquote>
<p>2.22 : 自我承诺</p>
</blockquote>
<blockquote>
<p>2.23 : Try to make some BIG changes to life but seems to back fire.
This is strange right? But when we try to change or step out of the
comfort zone, things WILL NOT GO AS I WISH. I mean, what's the point of
doing something, if this something is completely within my comfort zone?
The real way to fulfill self promis is to treat it as a long term
progress where I make mistakes. It's about not giving up and saying "let
this be" when I fail at the first time. It's about continuing even if I
want to just quit. It's sort of like when you cannot fulfill one promise
that you made to your best friend. The real way to solve this is to say
"here's how I can mend / I will do better next time, plz be patient with
me" rather than feeling that this is the end of the commitment and
friendship and say "Hey you know what, let's just put it there. Let's
just abandon this friendship just because of this ONE mistake".</p>
</blockquote>
<p>In short, treat yourself as a naughty child. Be patient with them and
give them the right directions. Don't give up. Always move on.</p>
]]></content>
      <categories>
        <category>Thoughts</category>
      </categories>
      <tags>
        <tag>Thoughts</tag>
      </tags>
  </entry>
  <entry>
    <title>Notes for CS2040S</title>
    <url>/2024/01/28/Notes-for-CS2040S/</url>
    <content><![CDATA[<p>Mainly questions that I encountered during my study for
CS2040S<Data Structures and Algorithms></p>
<span id="more"></span>
<h1 id="lecture-1-2-java-and-overall-data-structures">Lecture 1 &amp; 2
: Java and Overall Data Structures</h1>
<ol type="1">
<li>[U] How to implement a <strong>File System</strong>?</li>
</ol>
<p><img src="FileSystem.png" /></p>
<pre><code>1. Objects represent state (“nouns”), not actions (”verbs”)

2. What after folders?

3. Objects should be unitary, without dividing functionality.(What does this mean and why?)</code></pre>
<ol start="2" type="1">
<li><p><strong>First Principal of JAVA: Everything is an
object</strong></p>
<p>A class is a template for producing an object.</p>
<p>An object is an instance of the class.</p></li>
<li><p><strong>Second Principal of JAVA: Everything has a
type</strong></p>
<p><strong><a
href="https://docs.oracle.com/en/java/javase/11/docs/api/java.base/java/lang/String.html">Type
String</a></strong>:</p>
<ul>
<li><p>charAt(int index)</p></li>
<li><p>substring(int begin, int end)</p></li>
<li><p>toCharArray()</p></li>
<li><p>length()</p></li>
<li><p>compareTo(String otherString)</p></li>
<li><p>compareToIgnoreCase(String otherString)</p></li>
<li><p>equals(Object anObject)</p></li>
</ul></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">File</span> <span class="keyword">implements</span> <span class="title class_">IFile</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//Member(Instance) Variables.</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> “”;</span><br><span class="line">    <span class="type">FileData</span> <span class="variable">contents</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span>[] pageNumbers = <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">File</span><span class="params">(<span class="type">int</span> NumPages)</span>&#123;</span><br><span class="line">        pageNumbers = <span class="keyword">new</span> <span class="title class_">int</span>[numPages]; <span class="comment">//array constructer</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//Methods(Functions)</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">rename</span><span class="params">(String newName)</span>&#123;...&#125; <span class="comment">//signatures: parameters</span></span><br><span class="line">    FileData <span class="title function_">getData</span><span class="params">()</span>&#123;...&#125;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">setData</span><span class="params">(FileData newdata)</span>&#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Explain with a comment</span></span><br><span class="line"><span class="comment">// what your interface is for.</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">IFile</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Comments explain how to use interface</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">rename</span><span class="params">(String newName)</span>;</span><br><span class="line">    FileData <span class="title function_">getData</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">setData</span><span class="params">(FileData newdata)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol start="4" type="1">
<li><p><strong>Behaviour is public, Data is private.</strong></p>
<p>regular variables/functions are PER OBJECT</p>
<p>static variables/functions are PER CLASS // cannot access member
variable</p></li>
<li><p>Java program’s main method has to be declared
<strong>static</strong> because the keyword static allows main to be
called without creating an object. If omit the static keyword, it can
still compile.</p></li>
<li><p>Why is it incorrect to call Java "pass-by-reference" (should be
called <strong>pass-reference-by-value</strong> instead)? In the
following programme, i and j behaves differently. For j, lineA simply
changes the address of the parameter j0 is pointing to. Hence, the value
in j in main function does not change.(Think about the stack and heap
structure!) <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyInteger</span>() &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> value;</span><br><span class="line">    <span class="comment">//constructor omitted</span></span><br><span class="line">&#125;</span><br><span class="line">main()&#123;</span><br><span class="line">    <span class="type">MyIntger</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">10</span>;</span><br><span class="line">    <span class="type">MyInteger</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">10</span>;</span><br><span class="line">    addOne(i);</span><br><span class="line">    otherAddOne(j);</span><br><span class="line">    <span class="comment">//after execution: i.value = 11; j.value = 10;</span></span><br><span class="line">&#125;</span><br><span class="line">addOne(MyInteger i) &#123;</span><br><span class="line">    i.value ++;</span><br><span class="line">&#125;</span><br><span class="line">otherAddOne(MyInteger j0) &#123;</span><br><span class="line">    j0 = <span class="keyword">new</span> <span class="title class_">MyInteger</span>(j0.value + <span class="number">1</span>); <span class="comment">//lineA</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></li>
<li><p>f(n) = O(n) and g(n) = O(log n), what is h(n) = <span
class="math inline">\(f(n)^{g(n)}\)</span> ? <strong>the constat matters
in a exponent!</strong> Cannot tell because g(n) = k log(n) matters in
this situation.</p></li>
<li><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//I don&#x27;t know how I got O(n log n) out of this but the correct answer is O(n)</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">meanFunction</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">2</span> * meanFunction(n / <span class="number">2</span>) + niceFunction(n);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// This is O(n log n) instead of O(n ^ 2)</span></span><br><span class="line"><span class="comment">// j*=2 and j += 2 are different</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">1</span>; j &lt; i; j *= <span class="number">2</span>) &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// This is O(n ^ 2)</span></span><br><span class="line"><span class="comment">//string operations take o(Length) time(Strings are immutable!)</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    s += <span class="string">&quot;?&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//Improvement on the above code:</span></span><br><span class="line"><span class="type">StringBuilder</span> <span class="variable">sb</span> <span class="operator">=</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    sb.append(<span class="string">&quot;?&quot;</span>); <span class="comment">//O(1)</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> sb.toString();</span><br><span class="line"><span class="comment">//The order of growth is T(n),</span></span><br><span class="line"><span class="comment">//T(n) = SumOf(T(i), from 1 to sqrt(n)) + sqrt(n);</span></span><br><span class="line"><span class="comment">//Since T(n) is an increasing function, </span></span><br><span class="line"><span class="comment">//T(n) &lt;= sqrt(n) * T(sqrt(n)) + sqrt(n)</span></span><br><span class="line"><span class="comment">//we can guess that T(n) = O(n)</span></span><br><span class="line"><span class="comment">//Another wat to think is write out a few T(1), T(2), ... first</span></span><br><span class="line"><span class="comment">//Or from the trend of T(n) increases, guess that T(n) = O(n);</span></span><br><span class="line"><span class="comment">//For more efficient algrithm, see Sieve of Eratosthenes</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isPrime3</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (n&lt;<span class="number">2</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i=<span class="number">2</span>;i&lt;=sqrt(n);i++)&#123;</span><br><span class="line">        <span class="keyword">if</span> (isPrime3(i) &amp;&amp; isDivisible(n,i)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><ul>
<li>Abstract Data Types (ADTs) are interfaces, which are specifications
for behaviors without any implementation. E.g. Stack, Queue, Dictionary,
Set, Map</li>
<li>Data Structures are concrete implementations of ADTs. E.g. Array,
Linked List, Binary Tree, Hash Table, Heap</li>
<li>An ADT can be implemented by more than one data structure (E.g.
Queue can be implemented using either an array or a linked list)</li>
</ul></li>
<li><p>Linked List</p>
<ul>
<li>Advantages (compared to an array)
<ul>
<li>Variable size</li>
<li>Can easily add/delete elements anywhere</li>
</ul></li>
<li>Disadvantages
<ul>
<li>Difficulty in random access</li>
<li>Not guaranteed to be stored together in memory</li>
</ul></li>
</ul></li>
</ol>
<h1 id="lecture-3-4-peak-finding-searching">Lecture 3 &amp; 4 : Peak
Finding &amp; Searching</h1>
<ol type="1">
<li><p>[U] 2D Peak Finding: How to PROVE that it works?</p></li>
<li><p>Applications of binary search: Think of binary search
whenever:</p>
<ul>
<li>There is a sequence of "monotonic function" in blackbox. That is ,
for each v[i], for i &lt;= k, it returns false, for i &gt;= k, it
returns true.</li>
<li>We try to find the k which is the seperating point of a "valid"
value. (e.g. the "finding the pieces of homework-per-hour" problem <a
href="#fn1" class="footnote-ref" id="fnref1"
role="doc-noteref"><sup>1</sup></a> )</li>
</ul></li>
</ol>
<h1 id="lecture-5-6-sorting">Lecture 5 &amp; 6 : Sorting</h1>
<h2 id="bogosort">BogoSort</h2>
<ol type="1">
<li><strong>BogoSort Running Time</strong></li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Repeat:</span><br><span class="line"></span><br><span class="line">a) Choose a random permutation of the array A.</span><br><span class="line"></span><br><span class="line">b) If A is sorted, return A.</span><br></pre></td></tr></table></figure>
<p>What is the expected running time of BogoSort?</p>
<p>Ans: O(n * n!) // The <strong>n</strong> is important!</p>
<ol start="2" type="1">
<li><strong>Quantumn BogoSort</strong></li>
</ol>
<p><em>Remember QuantumBogoSort when you learn about non-deterministic
Turing Machines.</em> What?</p>
<ol start="3" type="1">
<li><strong>Maybe BogoSort</strong></li>
</ol>
<p>MaybeBogoSort(A[1..n]) <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Choose a random permutation of the array A.</span><br><span class="line">    If A[<span class="number">1</span>] is the minimum item in A then:</span><br><span class="line">        MaybeBogoSort(A[<span class="number">2.</span>.n])</span><br><span class="line">    Else</span><br><span class="line">    <span class="title function_">MaybeBogoSort</span><span class="params">(A[<span class="number">1.</span>.n])</span></span><br></pre></td></tr></table></figure> [U]What is the expected running
time of MaybeBogoSort?</p>
<pre><code>I guess it&#39;s something like 
    $\sum_&#123;k = 1&#125;^&#123;n&#125; (k-1) * (k-1)!$</code></pre>
<p>[U]But then, What is the bigO notation of this thing?</p>
<p>[U]Additionally, what is the difference between n! and <span
class="math inline">\(n^{n}\)</span>? What is the difference between n *
n! and (n+1)! ?</p>
<p>Stirling Apporximation:<a href="#fn2" class="footnote-ref"
id="fnref2" role="doc-noteref"><sup>2</sup></a></p>
<p><img src="Stirling%20Approximation" /></p>
<p>by this approximation, we can say that
<code>O(log(n!)) == O(nlog(n))</code></p>
<ul>
<li>[U]</li>
</ul>
<ol start="3" type="1">
<li>[U] List of the stability, runtime and performance difference on
nearly sorted and random arrays of different algorithms</li>
</ol>
<h2 id="classic-sorting-algorithms"><strong>Classic Sorting
Algorithms</strong></h2>
<h3 id="bogosort-1">BogoSort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">repeat n times:</span><br><span class="line">    <span class="keyword">for</span> j ¬ <span class="number">1</span> to n-<span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> A[j] &gt; A[j+<span class="number">1</span>] then <span class="title function_">swap</span><span class="params">(A[j], A[j+<span class="number">1</span>])</span></span><br></pre></td></tr></table></figure>
<h3 id="selection-sort">Selection Sort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> j ¬ <span class="number">1</span> to n-<span class="number">1</span>:</span><br><span class="line">    find minimum element A[j] in A[j..n] <span class="comment">//A[k]</span></span><br><span class="line">swap(A[j], A[k])</span><br></pre></td></tr></table></figure>
<h3 id="insertion-sort">Insertion Sort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">InsertionSort(A, n)</span><br><span class="line"><span class="keyword">for</span> j ¬ <span class="number">2</span> to n</span><br><span class="line">key ¬ A[j]</span><br><span class="line"><span class="comment">//Insert key into the sorted array A[1..j-1]</span></span><br><span class="line">i ¬ j-<span class="number">1</span></span><br><span class="line"><span class="keyword">while</span> (i &gt; <span class="number">0</span>) and (A[i] &gt;key)</span><br><span class="line">A[i+<span class="number">1</span>] ¬ A[i]</span><br><span class="line">i ¬ i-<span class="number">1</span></span><br><span class="line">A[i+<span class="number">1</span>] ¬ key</span><br></pre></td></tr></table></figure>
<p><em>In this case, since the insertion work is done from nearest
element to key(backwards), it behaves differently in a sorted
array</em></p>
<p>[U] Average-case analysis:</p>
<p><img src="InsertionSort.png" /></p>
<blockquote>
<p>On average, a key in position j needs to move j/2 slots backward (in
expectation).(WHY?)</p>
</blockquote>
<h3 id="mergesort">MergeSort</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">MergeSort(A, n)</span><br><span class="line"><span class="keyword">if</span> (n=<span class="number">1</span>) then <span class="keyword">return</span>;</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    X ¬MergeSort(A[<span class="number">1.</span>.n/<span class="number">2</span>], n/<span class="number">2</span>);</span><br><span class="line">    Y ¬MergeSort(A[n/<span class="number">2</span>+<span class="number">1</span>, n], n/<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">return</span> Merge (X,Y, n/<span class="number">2</span>);</span><br></pre></td></tr></table></figure>
<p>For the iterative MergeSort, we will sort the array in groups of
power of 2. In other words, we will first sort the arrays in pairs, then
merge into 4’s, 8’s and so on, until we have merged the entire array</p>
<p>[U] Master Theorem? Akra–Bazzi Method ? ##
<strong>Summary</strong></p>
<table>
<colgroup>
<col style="width: 29%" />
<col style="width: 16%" />
<col style="width: 16%" />
<col style="width: 23%" />
<col style="width: 14%" />
</colgroup>
<thead>
<tr class="header">
<th>Features</th>
<th>Bubble Sort</th>
<th>Selection Sort</th>
<th>Inerstion Sort</th>
<th>Merge Sort</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Time Complexity</td>
<td>O(n^2)</td>
<td>O(n^2)</td>
<td>O(n^2)</td>
<td>O(n * log(n))</td>
</tr>
<tr class="even">
<td>NearlySorted ? Random</td>
<td>O(n) &lt;</td>
<td>=</td>
<td>O(n) &lt;</td>
<td>=</td>
</tr>
<tr class="odd">
<td>Space Complexity</td>
<td>O(n)</td>
<td>O(n)</td>
<td>O(n)</td>
<td>O(n) (No need to allocate new arrays, can you previous ones instead)
/ O(n * log(n)) when it's MergeSort iteration</td>
</tr>
<tr class="even">
<td>Stability</td>
<td>1(swap only when elements are different)</td>
<td>0(e.g. [2, 2, 1])</td>
<td>1(A[i] &gt; key not &gt;=)</td>
<td>1(if implementation is designed carefully)</td>
</tr>
</tbody>
</table>
<p><strong>[U] Find a permutation of [1..n] where:</strong></p>
<p>• BubbleSort is slow.</p>
<p>• InsertionSort is fast.</p>
<p>Or explain why such array does not exist</p>
<p><strong>How do you count space?</strong></p>
<p>• Maximum space every allocated at one time?</p>
<p>• Total space ever allocated.</p>
<p><strong>[U] How “close to sorted” should a list be for InsertionSort
to be faster?</strong></p>
<p><strong>Slowest Sorting Algorithm</strong> <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Step 1:</span><br><span class="line">– Generate all the permutations of the input.</span><br><span class="line">Step 2:</span><br><span class="line">– Sort the permutations (by number of inversions).</span><br><span class="line">Step 3:</span><br><span class="line">– Return the first element in the sorted list of</span><br><span class="line">permutations.</span><br></pre></td></tr></table></figure></p>
<h2 id="quicksort">Quicksort</h2>
<h3 id="introduction">Introduction</h3>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">QuickSort(A[<span class="number">1.</span>.n], n)</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">1</span>) then <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        Choose pivot index pIndex.</span><br><span class="line">        p = partition(A[<span class="number">1.</span>.n], n, pIndex)</span><br><span class="line">        x = QuickSort(A[<span class="number">1.</span>.p–<span class="number">1</span>], p–<span class="number">1</span>)</span><br><span class="line">        y = QuickSort(A[p+<span class="number">1.</span>.n], n–p)</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">partition(A[<span class="number">1.</span>.n], n, pIndex) <span class="comment">// Assume no duplicates, n&gt;1</span></span><br><span class="line">    pivot = A[pIndex]; <span class="comment">// pIndex is the index of pivot</span></span><br><span class="line">    swap(A[<span class="number">1</span>], A[pIndex]); <span class="comment">// store pivot in A[1]</span></span><br><span class="line">    low = <span class="number">2</span>; <span class="comment">// start after pivot in A[1]</span></span><br><span class="line">    high = n+<span class="number">1</span>; <span class="comment">// Define: A[n+1] = ¥</span></span><br><span class="line">    <span class="keyword">while</span> (low &lt; high)</span><br><span class="line">        <span class="keyword">while</span> (A[low] &lt;= pivot) and (low &lt; high) <span class="keyword">do</span> low++;</span><br><span class="line">        <span class="keyword">while</span> (A[high] &gt; pivot) and (low &lt; high) <span class="keyword">do</span> high– – ;</span><br><span class="line">        <span class="keyword">if</span> (low &lt; high) then <span class="title function_">swap</span><span class="params">(A[low], A[high])</span>;</span><br><span class="line">    swap(A[<span class="number">1</span>], A[low–<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">return</span> low–<span class="number">1</span>;</span><br></pre></td></tr></table></figure>
<ol type="1">
<li>What if there are duplicates?</li>
</ol>
<p>[U] Two way partitioning (what happens for numbers on the right side?
Lec 6 slides)</p>
<p>One way partitioning - maintaining 4 parts of an array. * Quicksort
is not stable</p>
<p>[U] Is it possible to make quicksort stable by using an extra array
to partition rather than do swapping?</p>
<ol start="2" type="1">
<li><p>[U] Why is quicksort faster than merge sort?</p></li>
<li><p>[U] What if we use triple pivots</p></li>
</ol>
<h3 id="time-complexity-for-random-chosen-pivot.">Time complexity for
random chosen pivot.</h3>
<p><em>For an array of length n with k distinct arrays, given a good
pivot choosing algorithm, the time complexity would be O(n log
k)</em></p>
<h4 id="analysis-on-a-good-pivot">Analysis on a good pivot</h4>
<p>T(n) = T(n-k) + T(k) + n</p>
<p>Suppose <span class="math display">\[\frac{1}{10} n  &lt; k &lt;
\frac{9}{10} n\]</span></p>
<p>Time complexity: O(n * h), where h is the time of partition</p>
<p><span class="math display">\[(\frac{9}{10}) ^ {h} * n &gt;=
1\tag{1}\]</span></p>
<ul>
<li>Paranoid Quicksort(partition until a good partition)
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ParanoidQuickSort(A[<span class="number">1.</span>.n], n)</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">1</span>) then <span class="keyword">return</span>;    </span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="type">repeat</span></span><br><span class="line">            <span class="variable">pIndex</span> <span class="operator">=</span> random(<span class="number">1</span>, n)</span><br><span class="line">            p = partition(A[<span class="number">1.</span>.n], n, pIndex)</span><br><span class="line">        until p &gt; (<span class="number">1</span>/<span class="number">10</span>)n and p &lt; (<span class="number">9</span>/<span class="number">10</span>)<span class="type">n</span></span><br><span class="line"></span><br><span class="line">    <span class="variable">x</span> <span class="operator">=</span> QuickSort(A[<span class="number">1.</span>.p–<span class="number">1</span>], p–<span class="number">1</span>)</span><br><span class="line">    y = QuickSort(A[p+<span class="number">1.</span>.n], n–p)</span><br></pre></td></tr></table></figure></li>
</ul>
<p>[U] Non-paranoid quicksort works too?</p>
<p>[U] Why is it more important to fool the adversary then for the
algorithm to behave well on average cases(we would need time to do
randomization and find the A[k], but we don't need time to get A[1]) Or,
since the possibility of choosing a bad pivot is small, is the
efficiency of doing paranoid quicksort better then randomly
choosing?</p>
<h4 id="probability-theory-on-choosing-a-good-pivot">Probability Theory
on choosing a good pivot</h4>
<p>We only execute the repeat loop <span
class="math inline">\(\frac{n}{k}\)</span> times (in expectation) <a
href="#fn3" class="footnote-ref" id="fnref3"
role="doc-noteref"><sup>3</sup></a></p>
<h3 id="quicksort-optimization">Quicksort Optimization</h3>
<ol type="1">
<li><p>Switch to InsertionSort for small arrays.</p></li>
<li><p>Halt recursion early, leaving small arrays unsorted. Then perform
InsertionSort on entire array. (InsertionSort is fast on almost sorted
arrays, about O(n))</p></li>
<li><p><a
href="https://github.com/openjdk/jdk/blob/15196325977254ee73d96bf99be62c520de59af9/src/java.base/share/classes/java/util/DualPivotQuicksort.java#L2186">Java
QuickSort Implementation</a> uses a two pivot quicksort.</p>
<ul>
<li>Sort the k pivots in the pivot array K</li>
<li>Do QuickSelect(e, K) on every element e in the original array
(partitioning)</li>
<li>Recurse.</li>
</ul>
<p>The time complexity is T(n) = O(nlogk) + k * T(n / k) which is O(n
log n). The performance of 2 and 3 pivot QuickSort are actually better
than regular QuickSort because <em>2-pivot quicksort takes benefits of
modern computer architecture</em>.</p></li>
</ol>
<h2 id="order-statistics">Order Statistics</h2>
<p>Find <span class="math inline">\(k^{th}\)</span> smallest element in
an unsorted array:</p>
<ol type="1">
<li>sort and return A[k]</li>
<li><strong>Minimize sorting</strong></li>
</ol>
<p><em>You can even do paranoid sorting</em></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Select(A[<span class="number">1.</span>.n], n, k)</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">1</span>) then <span class="keyword">return</span> A[<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">else</span> Choose random pivot index pIndex.</span><br><span class="line">        p = partition(A[<span class="number">1.</span>.n], n, pIndex)</span><br><span class="line">        <span class="keyword">if</span> (k == p) then </span><br><span class="line">            <span class="keyword">return</span> A[p];</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (k &lt; p) then</span><br><span class="line">            <span class="keyword">return</span> Select(A[<span class="number">1.</span>.p–<span class="number">1</span>], k)</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (k &gt; p) then</span><br><span class="line">            <span class="keyword">return</span> Select(A[p+<span class="number">1</span>], k – p)</span><br></pre></td></tr></table></figure>
<p><strong>Time Complexity: O(n)</strong></p>
<h2 id="other-sorting-algorithms">Other Sorting Algorithms</h2>
<h3 id="counting-sort">Counting Sort</h3>
<p><a href="https://www.geeksforgeeks.org/counting-sort/">Counting
Sort</a> is useful for sorting arrays that contains integers from 0 - M,
where M is relatively small.</p>
<ul>
<li>Time complexity: O(n + M)</li>
</ul>
<p>[U] Check the website!</p>
<h3 id="bitsort">BitSort(?)</h3>
<ol type="1">
<li>Regular version</li>
</ol>
<ul>
<li>In-place-sorting agorithm for arrays of 0s and 1s: Do Quiksort
Partition on the array, with two pointers, one at each side:The
0-pointer will advance to the right up until it finds the first 1; vice
versa.</li>
<li>For an integer of k bits, do k times the In-place-sorting, starting
from the highest bit.</li>
<li>time complexity is O(kn). However! since integers are stored in 64
bits, only when n &gt; <span class="math inline">\(2^{64}\)</span>, this
would beat Quicksort!</li>
</ul>
<ol start="2" type="1">
<li>Optimization:</li>
</ol>
<ul>
<li><p>Don't represent integers in binary, rather, in M - base numbering
systems, where M is realtively small. Now, do Counting Sort <span
class="math inline">\(log_M\)</span> n times. the time complexity would
be O(n * logM n). However, it still requires n to be relatively
large.</p></li>
<li><p>TradeOff: Space! Lets say we use 8-base algorithm. It will take
256 Integers worth of space to do Counting Sort! <em>Also the fact that
the sorting is not in-place might take longer than you think</em> ([U]
Test it out!)</p></li>
</ul>
<h1 id="lecture-08-trees">Lecture 08 Trees</h1>
<h2 id="dynamic-data-structures">Dynamic Data Structures</h2>
<p>Operations that create a data structure - build (preprocess)</p>
<p>Operations that modify the structure - insert - delete</p>
<p>Query operations - search, select, etc.</p>
<h2 id="tree-basic-concepts">Tree Basic Concepts</h2>
<ol type="1">
<li><p>[U] What determines shape? • Order of insertion • Does each order
yield a unique shape? NO – # ways to order insertions: n! – # shapes of
a binary tree? ~4n (Catalan numbers: Catalan Numbers Cn = # of trees
with (n+1) nodes Cn = # expressions with n pairs of matched
parentheses)</p></li>
<li><p>Successor:</p>
<figure>
<img src="Tree_Successor.png"
alt="If you search for a key not in a tree, you would either find its successor or its predecessor" />
<figcaption aria-hidden="true">If you search for a key not in a tree,
you would either find its successor or its predecessor</figcaption>
</figure>
<p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Successor(<span class="type">int</span> key) &#123; <span class="comment">// successor function for key (in / not in tree)</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> search(key); <span class="comment">//either return successor or predecessor</span></span><br><span class="line">    <span class="keyword">if</span>(result &gt; key) <span class="keyword">return</span> result;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> SuccessorKeyInTree(result);</span><br><span class="line">&#125;</span><br><span class="line">SuccessorKeyInTree(<span class="type">int</span> key) &#123;<span class="comment">// search for successor when key is in tree</span></span><br><span class="line">    <span class="keyword">if</span> (key has right child ) &#123;</span><br><span class="line">        <span class="keyword">return</span> rightChild;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> ((parent != <span class="literal">null</span>) &amp;&amp; (child == parent.rightTree)) &#123;</span><br><span class="line">        child = parent;</span><br><span class="line">        parent = child.parentTree; <span class="comment">// note that here child has become parent</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> parent;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></li>
<li><p>Deleting <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (key.childNumber == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">//simply delete.</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (key.childNumber == <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="comment">//make the child of key substitude child</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">//key has two child -&gt; key has right child -&gt; the successor is the min in the right child -&gt; sucessor has no left child.</span></span><br><span class="line">    substitude successor with its child and substitude key with successor.</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p></li>
</ol>
<h2 id="keeping-trees-balanced">Keeping Trees Balanced</h2>
<h3 id="concept">Concept</h3>
<p>Key definition: A tree is <strong>balanced</strong> if h = O(logN)
(By default: log(n) –1 ≤ h ≤ n)</p>
<ol type="1">
<li>Augment</li>
<li>Define Balance Condition</li>
<li>Maintain Balance</li>
</ol>
<h3 id="avl-trees">AVL Trees</h3>
<h4 id="proof-of-balance">Proof of Balance</h4>
<ol type="1">
<li><p>node v is height-balanced if |v.left.height – v.right.height|
&lt;= 1</p></li>
<li><p>A height-balanced tree with n nodes has at most height h &lt;
2log(n)</p>
<p>Proof: = proof that every height-balanced tree with height h has at
least n nodes <em>(Since every height-balanced tree with height h has at
least n &gt; 2h/2 nodes, every height-balanced tree with n nodes has
height h &lt; 2log(n).)</em></p>
<pre><code> $$n_h &gt;= 1 + n_&#123;h-1&#125; + n_&#123;h-2&#125; &gt;= 2 * n_&#123;h-2&#125; &gt;= 2^&#123;n/2&#125; * n_&#123;n-n&#125; = 2^&#123;n/2&#125;$$</code></pre></li>
</ol>
<p><em>Interesting Property: a maximal imbalanced AVL tree(AVL treewith
the minimum possible number of nodes given its height h.)has all its
subtrees to be maximally imbalanced.</em></p>
<p>What else can we store instead of height: height difference between
the left and right subtrees! [U] How do we implement that? how do we
find out whether the tree is not balanced?</p>
<p><em>Proof: if S(h) = the minimum possible number of nodes: S(h) = S(h
− 1) + S(h − 2) + 1.</em></p>
<h4 id="maintaining-height-balance">Maintaining Height-Balance</h4>
<figure>
<img src="RightRotation.png" alt="RightRotation" />
<figcaption aria-hidden="true">RightRotation</figcaption>
</figure>
<p>After insertion, start at bottom, work your way up.</p>
<p>Assume A is the lowest node violating valance property. Assuming that
A is <strong>left heavy</strong>:</p>
<ol type="1">
<li>B is balanced</li>
</ol>
<figure>
<img src="TreeRotationCase1.png"
alt="B is balanced: do righ rotation on A" />
<figcaption aria-hidden="true">B is balanced: do righ rotation on
A</figcaption>
</figure>
<ol start="2" type="1">
<li>B is left-heavy</li>
</ol>
<figure>
<img src="TreeRotationCase2.png" alt="B is left-heavy" />
<figcaption aria-hidden="true">B is left-heavy</figcaption>
</figure>
<ol start="3" type="1">
<li>B is right heavy</li>
</ol>
<figure>
<img src="TreeRotationCase3_1.png"
alt="Left Rotate B - A and C are still out of balance" />
<figcaption aria-hidden="true">Left Rotate B - A and C are still out of
balance</figcaption>
</figure>
<figure>
<img src="TreeRotationCase3_2.png" alt="Right Rotate A - Solved!" />
<figcaption aria-hidden="true">Right Rotate A - Solved!</figcaption>
</figure>
<p>[S] what about nodes higher than that?</p>
<pre><code>- In Case 2 &amp; 3, we reduce the height by 1, thus the upper must be balanced. ([U] Why doesn&#39;t that unbalance the balanced node?)

- In Case 1, Case 1 would never appear when doing an insertion.</code></pre>
<p>[S] What about deletion!</p>
<p>For deletion, we walk up tree, check for balance and rotate every
node if needed. It is NOT sufficient to only fix lowest out-of-balance
node. [U] Why?</p>
<figure>
<img src="BalanceButNotHeightBalance.png" alt="[U] Why?" />
<figcaption aria-hidden="true">[U] Why?</figcaption>
</figure>
<p>[U] Using rotations you can create every possible tree shape?</p>
<p>[U] What if for deletion, we don't rotate immediately but leave it in
the tree and do rotations afterwards? (Amortized performance)</p>
<p>[U] Instead of storing height from every node, only store difference
in height from parent - What's the use?</p>
<h2 id="augmenting-trees">Augmenting Trees</h2>
<p>Properties: properties that contains information about the subtrees
(or information that can be updated by walking up/down the tree) tends
to be easy to maintain; properties that contain the full information
about the tree tend to be hard to maintain overall(e.g. if I want to do
selection, what properties do I need: weight of subtree - information
that can be uploaded VS index - full information about the tree) (L10
1h10min)</p>
<h3 id="dynamic-order-statistics">Dynamic Order Statistics</h3>
<p>[U] Compare the time complexity of using sorted array and a tree?
Additionally, compare the behavior of storing index and storing weight
in a tree?</p>
<ol type="1">
<li><p>Find the rank of a node <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">rank(node)</span><br><span class="line">    rank = node.left.weight + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span> (node != <span class="literal">null</span>) <span class="keyword">do</span></span><br><span class="line">        <span class="keyword">if</span> node is left child then</span><br><span class="line">            <span class="keyword">do</span> nothing</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> node is right child then</span><br><span class="line">            rank += node.parent.left.weight + <span class="number">1</span>;</span><br><span class="line">        node = node.parent;</span><br><span class="line">    <span class="keyword">return</span> rank;</span><br></pre></td></tr></table></figure></p></li>
<li><p>Maintaining weight during insertion and rotation.</p></li>
</ol>
<figure>
<img src="MaintainingWeight.png"
alt="Maintaining weight during rotations" />
<figcaption aria-hidden="true">Maintaining weight during
rotations</figcaption>
</figure>
<h2 id="tree-traversal">Tree Traversal</h2>
<h3 id="dfs-and-bfs">DFS and BFS</h3>
<h3 id="introduction-1">Introduction</h3>
<p>[U] Wait, where did this come from?</p>
<h3 id="non-recursive-bfs-and-dfs">Non-recursive BFS and DFS</h3>
<h4 id="stack---dfs">Stack - DFS</h4>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">push(root);</span><br><span class="line"><span class="keyword">while</span> (!stack.empty()) &#123;</span><br><span class="line">    <span class="type">node</span> <span class="variable">curr</span> <span class="operator">=</span> stack.pop();</span><br><span class="line">    push(curr.children);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="tree-operation-conclusion">Tree Operation conclusion</h2>
<ol type="1">
<li>modifying:
<ul>
<li>insert: O(h)</li>
<li>delete: O(h)</li>
</ul></li>
<li>Query Operations:
<ul>
<li>search: O(h)</li>
<li>predecessor / successor: O(h)</li>
<li>findMax / findMin: O(h)</li>
<li>Traversal: O(n)</li>
</ul></li>
</ol>
<h3 id="trie">Trie</h3>
<ol type="1">
<li>Costs(h for the height of trie, L for the length of word)
<ul>
<li>search: time - O(L)</li>
<li>store: space - O(L) <em>What does prof mean by tries take up more
space because it involves more overhead here?(L10, p90)</em></li>
</ul></li>
<li>String dictionaries
<ul>
<li>Searching</li>
<li>Sorting / enumerating strings</li>
</ul>
<em>Partial string operations:</em>
<ul>
<li>Prefix queries: find all the strings that start with pi.</li>
<li>Long prefix: what is the longest prefix of “pickling” in the
trie?</li>
<li>Wildcards: find a string of the form “pi??le” in the trie.</li>
</ul></li>
</ol>
<h3 id="interval-queries">Interval Queries</h3>
<h4 id="goal">Goal:</h4>
<ul>
<li>Solutions where space is linear (or near linear) in the number of
things stored (i.e., intervals).</li>
<li>Operations are logarithmic in # of things stored.</li>
</ul>
<h4 id="augment">Augment</h4>
<p>store the maximum of right end point!</p>
<h4 id="implement">Implement</h4>
<h5 id="search">search:</h5>
<figure>
<img src="SearchIntervalTree.png" alt="sample interval tree" />
<figcaption aria-hidden="true">sample interval tree</figcaption>
</figure>
<p>when we walk down the nodes to(17,19), the information that we need
are: 17(left interval), 25(max = left subtree)</p>
<ul>
<li><p>Case 1: x &gt; 25 (max of left subtree):</p>
<p>Go right. (the node is either in right tree or not in tree!)</p></li>
<li><p>Case2: 17 &lt; x &lt;= 25</p>
<p>Go left. (there must be atleast one node that contains x!)</p></li>
<li><p>Case3: x &lt; 17</p>
<p>Go left. (there is 0 possibility that the right subtree contains
x!)</p></li>
</ul>
<p>In three cases we are safe to go (the direction we go contains the
key / the direction we don't go does not contain key)</p>
<h5 id="insertion-rotation">insertion &amp; rotation</h5>
<p>easy to maintain</p>
<h5 id="others">Others</h5>
<p>[U] Extension: List all intervals that overlap with point?</p>
<h3 id="orthogonal-range-searching">Orthogonal Range Searching</h3>
<h4 id="introduction-2">Introduction</h4>
<p>Are there any good restaurants within one block of me - search for
the points in a given box</p>
<h4 id="sample-one-dimension">Sample: One-Dimension</h4>
<h5 id="strategy">Strategy</h5>
<ol type="1">
<li>Use a binary search tree and store all the points in the leaves of a
tree. Each internal node stores the MAX of any leaf in the left
sub-tree.</li>
</ol>
<figure>
<img src="OneDimensionORS.png" style="width:60.0%" alt="Sample Tree" />
<figcaption aria-hidden="true">Sample Tree</figcaption>
</figure>
<ol start="2" type="1">
<li><p>Do Search(10, 50): (Time complexity: <strong>O(log(n) +
k)</strong>, where k is the number of items)</p>
<ul>
<li>Find Split Node: the highest node that search includes both left and
right subtrees(in this case, 25) <strong>O(logn)</strong></li>
<li>Do left and right traversal on the left and right tree.
<strong>O(k)</strong></li>
<li>Left Traversal: The search interval for a left-traversal at node v
includes the maximum item in the left subtree of v. <figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LeftTraversal(v, low, high) &#123;</span><br><span class="line">    <span class="keyword">if</span> (low &lt;= v.key) &#123;</span><br><span class="line">        all-leaf-traversal(v.right); </span><br><span class="line">        <span class="comment">//change all-tree-traversals to total += v.right.ount</span></span><br><span class="line">        <span class="comment">//if just need to know how many nodes are with the range</span></span><br><span class="line">        LeftTraversal(v.left, low, high);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">// left traversal on the right subtree</span></span><br><span class="line">        LeftTraversal(v.right, low, high);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">RightTraversal(v, low, high) &#123;</span><br><span class="line">    <span class="keyword">if</span> (v.key &lt;= high) &#123;</span><br><span class="line">        all-leaf-traverasal(v.left);</span><br><span class="line">        RightTraversal(v.right, low, high);</span><br><span class="line">    &#125;  <span class="keyword">else</span> &#123;</span><br><span class="line">        RightTraversal(v.left, low, high);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul></li>
<li><p>BuildTree time complexity : O(n log n) ([U] why?)</p></li>
<li><p>Dynamic updates: Rotation ([U] how?)</p></li>
</ol>
<h4 id="two-dimension">Two-Dimension</h4>
<ol type="1">
<li>Idea:</li>
</ol>
<ul>
<li>1: Create a 1d-range-tree on the x-coords.</li>
<li>2: Store a y-tree at each x-node containing all the points in the
sub-tree.</li>
</ul>
<figure>
<img src="TwoDimensionRangeSearching.png"
alt="Too much space for all the nodes! - use two-dimensional Trees!" />
<figcaption aria-hidden="true">Too much space for all the nodes! - use
two-dimensional Trees!</figcaption>
</figure>
<p>[U] don't quite understand.</p>
<h3 id="k-d-tree">k-d Tree</h3>
<p>space partitioning data structure for organising points into a
𝑘-dimensional space</p>
<h2 id="b-trees">B-trees</h2>
<h3 id="a-b-trees">(a, b) trees</h3>
<h4 id="rules">Rules</h4>
<ul>
<li>2 ≤ a ≤ (b + 1)/2, where a and b refers to the minimum and maximum
number of <strong>children</strong> in a internal node(not root /
leaf).</li>
</ul>
<figure>
<img src="abtreeChildPolicy.png" alt="(a,b)tree child policy" />
<figcaption aria-hidden="true">(a,b)tree child policy</figcaption>
</figure>
<ul>
<li><p>every node should follow that: numberOfChild = numberOfKey + 1.
For a node with k keys (v1,v2,...,vk), the subtrees(t1,t2,...,tk+1)
should follow that ： First child t1 has key range ≤ v1； Final child
tk+1 has key range &gt; vk； All other children ti where i ∈ [2, k] has
key range (vi−1, vi)</p></li>
<li><p>All leaves should have the same depth.</p></li>
</ul>
<h4 id="manipulation">Manipulation</h4>
<ol type="1">
<li><p>Is ab tree balanced? <img src="abtreeBalance.png"
alt="Is ab tree balanced?" /></p></li>
<li><p>Insertion:</p></li>
</ol>
<p>Solution 1: (1) At first put the element in its correct place in the
node (which is in sorted order)</p>
<ol start="2" type="1">
<li><p>If the node overflows, then find the median ( = if is even and if
is odd ) key in that node.</p></li>
<li><p>Put the median key in the parent and split the current
node</p></li>
</ol>
<p>Solution 2: Proactive Approach</p>
<p>Preemptively split a node whe it is full so that it does not overflow
in the future.</p>
<ol start="3" type="1">
<li>Deletion:</li>
</ol>
<ol type="1">
<li>Case 1: Problem with Orphan Children.</li>
</ol>
<figure>
<img src="abtreeOrphan.png" alt="Delete 13" />
<figcaption aria-hidden="true">Delete 13</figcaption>
</figure>
<pre><code>In this case, we replace the deleted node either with the predecessor or successor key</code></pre>
<ol start="2" type="1">
<li>Case 2: Problem with downflow</li>
</ol>
<figure>
<img src="abtreeDownFlow.png" alt="Delete 23" />
<figcaption aria-hidden="true">Delete 23</figcaption>
</figure>
<p>If at least one sibling has at least a keys, then donate the key to
the other sibling</p>
<p>If none have enough keys, then merge the two siblings and the parent
together.</p>
<p><img src="abtreeDownFlow2.png" alt="Downflow" /> <img
src="abtreeDownFlow3.png" alt="Down flow again" /></p>
<p><img src="abtreeDownFlow4.png" alt="Another example" /> <img
src="abtreeDownFlow5.png" /> <img src="abtreeDownFlow6png" /></p>
<h3 id="b-trees-1">B-trees</h3>
<p>B-trees are simply (B, 2B)-trees. That is to say, they are a
subcategory of (a, b)-trees such that a = B and b = 2B.</p>
<ol type="1">
<li>Cost</li>
</ol>
<ul>
<li><p>All the costs of searching a key list / splitting a node /
merging / sharing B-tree nodes are O(1)!</p></li>
<li><p>The cost of searching a B-tree / inserting / deleting in a B tree
is O(loga n)!</p></li>
</ul>
<h1 id="others-1">Others</h1>
<ol type="1">
<li>log2n = O( log10n ) Correct! We can easily switch between bases by
multiplying by a constant.</li>
</ol>
<h1 id="problems-in-tutorials">Problems (in tutorials)</h1>
<h2 id="mountain-stack">Mountain stack</h2>
<p>A mountains consists of <strong>n</strong> hills, aligned linearly.
Each hill <strong>i</strong>, stands at a height of a[i]. Standing at
hill i, you’ll see a point j kilometers away if and only if all hills
from i−j to i are no taller than a[i] meters. For each hill i, determine
the maximum distance you can see from it(distance is "-1" if no hill
blocks your sight). <em>(Hint: try using a stack to achieve O(n) time
complexity)</em></p>
<p><strong>Answer</strong>: Use a monotonic decreasing stack: - For
every element at index i in the array: <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">boolean</span> <span class="variable">isInfinity</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line">While(!stack.empty) &#123;</span><br><span class="line">    curr = stack.peek(); <span class="comment">//get the top object without popping it</span></span><br><span class="line">    <span class="keyword">if</span>(curr.height &gt;= h[i])&#123;</span><br><span class="line">        isInfinity = <span class="literal">false</span>;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        d[i]++;</span><br><span class="line">        stack.pop();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(isInfinity) d[i] = -<span class="number">1</span>;</span><br></pre></td></tr></table></figure> <em>[U] This is
somehow incorrect. But the idea is similar. pls correct this.</em></p>
<p>How to come up with this: <strong>Gather only the information you
need</strong>! Information for any hill i that is lower than another
hill j where j &gt; i can be eliminated!</p>
<h2 id="chicken-rice-competetion">Chicken Rice Competetion :</h2>
<ul>
<li>You have in front of you n plates of chicken rice, each plate
contains n bites of chicken rice. Supppose you want to find the median
chicken rice. Design an algorithm to maximize the amount of remaining
chicken rice on the median plate.</li>
<li>Solution 1: Run QuickSelect: on one level of QuickSelect, the
expectation of time of bitten on any plate is (1 / n) * (n - 1) + (1 - 1
/ n) * 1 &lt;= 2. Thus the expected consumption of chicken rice is O(2
log n) bites.</li>
<li>Solution 2: Inserting each plate into an AVL tree takes O(log n)
comparisons. (However, if median is the root of AVL tree, there would be
O(n) moves!)</li>
<li><em>In fact, using a randomized algorithm, this can be solved with
only O(log log(n)) bites to the median plate, but that is much, much
more complicated</em> [U]</li>
</ul>
<h2 id="weight-selectioneonomic-research">Weight Selection(Eonomic
Research):</h2>
<ul>
<li>You are an economist doing a research on the wealth of different
generations. Each row of data you've gained consists of a unique
identifier, an age, and a number that represents their amount of wealth.
Your goal is to divide the dataset into “equi-wealth” age ranges. That
is, given a parameter k, you should produce k different age ranges A1,
A2, ..., Ak with the following properties:
<ol type="1">
<li>All the ages of people in set <span
class="math inline">\(A_j\)</span> should be &lt;= the ages of people in
<span class="math inline">\(A_{j+1}\)</span>.</li>
<li>The sum of wealth in each set should be (roughly) the same
(tolerating rounding errors if k does not divide the total wealth, or
exact equality is not attainable).</li>
</ol></li>
</ul>
<p>You should assume that the given k is relatively small while the
dataset is very large . (i.e. O(nk) &lt; O(n log n))</p>
<p>Design the most efficient algorithm you can to solve this problem.
Optionally, consider the case where rows can consist of the same ages,
versus the case where rows have unique ages. - Solution: a weight
selection problem. We are asking to find the 1/k,2/k,...,k−1/k order
statistics of the weighted sum 1. Finding a single Weight Order
Statistic. - First, use QuickSelect to find the median aged dataset, and
then partition around the median - sum the totals on the left and right
halves. (DO the sum while partitioning to save time!) If target weight
&gt; left sum, subtract left sum weight from your target weight and
search on right half...etc. This is O(n) time. 2. repeat for the k - 1
targets and <em>Do one final partitioning to build your output lists([U]
why is this nessary)</em> This is O(nk) time.</p>
<ul>
<li><p>Solution 2: Instead of finding the median, we use a random pivot.
Each time we partition, we keep track of the sum of the wealth to the
left of the selected pivots! We do Quickselect O(k) times and each
selection takes O(n) times. ([U] Needs further explanation, see tutorial
slides w6 "better solution") [U] Why is this better than Solution
1?</p></li>
<li><p>Solution 3: Find k order statistics all at once! When random
pivot was selected, say its 2 * <span
class="math inline">\(\bar{w}\)</span>, then we recurse on the left to
find the 1st order-statistics and recurse on the right to find the 3rd
order-statistics! In this case, we get a rough overall runtime of O(n
log k) ([U] Needs further explanation, see tutorial slides w6 "better
solution")</p></li>
<li><p>[U] How about naiive solution?</p></li>
</ul>
<h2 id="height-of-binary-tree-after-subtree-removal">Height of Binary
Tree After Subtree Removal</h2>
<ul>
<li><p>Description: You are given a <strong>binary tree</strong> with n
nodes. You are given m queries, where queries[i] asks the height of the
tree after deletion of the subtree containing the ith node. What is the
optimized solution?</p></li>
<li><p>Solution 1 : Build a tree. for each query, walk up the tree and
try to update the node's height. O(log n * m) ([U] Why does the slides
say its O(n * m)?)</p></li>
<li><p>Solution 2: <em>Depth of a node refers to the number of edges in
the simple path from root to the node</em></p>
<ol type="1">
<li>preprocess the tree to get its depth using DFS and height using
in-order-traversal.</li>
<li>For ith query, we look at all the nodes with the same depth of ith
node and find the maximum height among them. The answer to the query
would be <code>node[i].depth + max_height</code>.</li>
<li>SO how do we group the nodes of the same depth? We do this by
iterating the nodes in in-order-traversal([U] wut?). For each group of
nodes in the same depth, we only require the maximum height and second
maximum height. To do this we need O(n) time([U] wut?).</li>
<li>In this algorithm, we only need O(1) for each query. The overal time
complexity would be O(n + m)</li>
</ol></li>
</ul>
<p>[U] <em>There is another method based on Euler Tour Tree (which is
out of syllabus of CS2040S). You may find more details about the data
structure on</em></p>
<p>https://courses.csail.mit.edu/6.851/spring07/scribe/lec05.pdf and
http://web.stanford.edu/class/archive/cs/cs166/cs166.1166/lectures/17/Small17.pdf.</p>
<p><em>The data structure provide a neat alternative solution and
support for deleting multiple subtrees.</em></p>
<h2 id="safely-evaluating-average-pay">Safely Evaluating average
pay!</h2>
<ol type="1">
<li>Question 1</li>
</ol>
<ul>
<li>Come up with an algorithm that will allow the students to determine
the average pay of the group without revealing their own salaries to any
of the other members in the group</li>
<li>Solution 1: Everyone pick a random number and add it to there
salery. 1st person passes the sum to the 2nd person, the 2nd person add
their sum to the number the 1st one passed ... Until everyone's sum adds
up. Then they deduct the number one by one.</li>
</ul>
<ol start="2" type="1">
<li>Extention:</li>
</ol>
<ul>
<li>what happens if k members of the group decide to collude and share
information. Is your algorithm still able to preserve the privacy of all
the members, or could the salary for some of the members be leaked</li>
<li>Solution 1 will not work! the (i-1)th and (i+1)th person can easily
calculate the ith people's salery! ([U] is there a way to manipulate the
order of adding and subtracting such that there is no way they can do
that?)</li>
<li>Solution 2: Divide the salery into n pieces and each person give the
ith piece of their salery to the ith people! Even if everyone in the
house except for the kth person cheat and try to calculate kth person's
salery, they cannot do so! ([U] How did you come up with that!)</li>
<li>Way to come up with a good algorithm: [U] ?
<ol type="1">
<li>How you (re)define the problem</li>
<li>Determines what are the metrics of success</li>
<li>How to iterate a solution?</li>
</ol></li>
</ul>
<h2 id="pancake-flipping">Pancake flipping!</h2>
<ol type="1">
<li><p>Description: Try to serve pancakes a stack sorted by size. You
can only flip the pancakes at the top of the pancake stack. <img
src="PancakeFlipping.png" alt="Flipping top 3 pancakes" /></p></li>
<li><p>Show that any pancake flipping algorithm requires at least n
flips. What do we call such properties of the problem? [U] Check out
recitation 2</p></li>
</ol>
<ul>
<li>A lower bound of the algorithm!</li>
<li>Consider a possible worst case! <img src="PancakeWorstCase.png" />
Why is this the worst case? [U] Worst case should differ for different
algorithms?
<ul>
<li>Feature 1: if two consecutive pancakes in the sorted pancake stack
are together in the unsorted stack then we can treat those pancakes as
one pancake either with burnt side up or down based on their order. This
means that if 2 4 are adjacent in the original pancake, we must use at
least one flip between 2 and 4 to seperate them!</li>
<li>Feature 2: If the largest pancake is at the bottom we can simply
ignore it.</li>
<li>By Feature 1, we need at least n - 1 flips. By feature 2, we need at
least 1 additional flip to seperate the bottom one and the table! Thus
the lower bound is at least n flip.</li>
</ul></li>
</ul>
<p>[U] Check out recitation 2 on more about pancake flipping.</p>
<h2 id="dna-swapping">DNA Swapping</h2>
<ol type="1">
<li>Description</li>
</ol>
<ul>
<li>We are doing sorting when only calculating the cst of swapping
subarrays. The cost of swapping subarrays of length k would cost
O(k).</li>
</ul>
<ol start="2" type="1">
<li>What if we are sorting an array of two element? - Do mergesort!</li>
<li>What if the input string comprises of arbitrary characters without
duplicates.</li>
</ol>
<ul>
<li>Wait, we can do this using the solution in 2! partition is the same
as sorting an array of two kinds of elements! The time complexity is
T(n) = 2 * T(n / 2) + O(n log n). [U]</li>
</ul>
<h2 id="random-permutation">Random Permutation</h2>
<ol type="1">
<li><p>Given an array of n items, we need to return a random permutation
of the given array.</p></li>
<li><p>How do we evaluate the performance of a random permutation
algorithm!</p></li>
</ol>
<ul>
<li>The possibility of each permuatation to appear in the array should
be n!</li>
</ul>
<ol start="3" type="1">
<li><p>Consider the Fisher-Yates / Knuth Shuffle algorithm: Time
Complexity O(n) and Space complexity O(1)! [U] why is space complexity
O(1)</p></li>
<li><p>Solution 2: assign a random number to each element and sort
it!</p></li>
</ol>
<ul>
<li>Question: what if we meet duplicate numbers?</li>
<li>If the sorting is stable, simply treat the earlier occurence to be
the smaller one([U] Is this still random? )</li>
<li>If not, the sorting would not be stable!</li>
</ul>
<p>[U] Check the ppt # Puzzles:</p>
<ol type="1">
<li><p>[U]Sicherman Dice</p></li>
<li><p>[U]Puzzle of the Week: Bubbly (Courtesy: MIT Puzzle Hunt,
2019)</p></li>
<li><p>[U]Treasure Island:</p></li>
</ol>
<p>You choose a set of keys.</p>
<p>You can test whether all the good keys are in the set (and
possibly</p>
<p>some extra keys). If even one bad key is in the set, then it
fails.</p>
<p>Goal: identify exactly the good keys.</p>
<ol start="4" type="1">
<li><p>PotW: Gambling for Profit? (Lecture 07 p121)</p></li>
<li><p>Prisoners! (L10)</p></li>
</ol>
<hr />
<p>** Updated: Lecture 11 p135</p>
<p>tut 2</p>
<p>[U] optional questions in tutorials.</p>
<p>[U] Check the open <a
href="https://github.com/4ndrelim/data-structures-and-algorithms">resourse</a></p>
<p>[U] Check this out: <a
href="https://visualgo.net/training">visualAlgo</a></p>
<p>[U] HashTable - Tutorial Slides W6 - problem 2 -credit CS2109S</p>
<p>[U] Optional problems in tuts and recs</p>
<p>There are some useful advice on midterms in Week 6 tut materal</p>
<section id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p>You have n piles of homework and the ith pile has
<strong>piles[i]</strong> pieces of homework. You have
<strong>h</strong> hours left before all your homework is due. Try to
find out the minimal <strong>k = “pieces of homework”-per-hour</strong>
while finishing everything on time that follows the below constraints:
At every hour, choose a pile of homework, If the pile has less than k
pieces of homework remaining, you do not start on the next pile during
the same hour. how you can find the minimum integer k such that you can
finish all your homework withinh hours? <em>(Hint: v[i] is the function
of whether you can finish on time, and k is the seperating point.
)</em><a href="#fnref1" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
<li id="fn2"><p>Another way to prove this is : <img
src="nlognisn.png" /><a href="#fnref2" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
<li id="fn3"><p><span class="math inline">\(\color{green}{SOMETHING
RELATED}\)</span>: How many flips to get at least one head? : <span
class="math display">\[E[X] = \frac{1}{2}(1) + \frac{1}{2} (1 + E[X])
\tag{1}\]</span> (this is useful for infinity E[x] problems)<a
href="#fnref3" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
      <categories>
        <category>Notes</category>
        <category>CS</category>
        <category>Algorithm</category>
      </categories>
      <tags>
        <tag>ComputerScience</tag>
        <tag>Notes</tag>
        <tag>Study</tag>
        <tag>NUS</tag>
        <tag>Java</tag>
        <tag>Algorithm</tag>
        <tag>Questions</tag>
      </tags>
  </entry>
  <entry>
    <title>Cheat Sheet for Unix CLI</title>
    <url>/2024/01/24/Cheat-Sheet-for-Unix-CLI/</url>
    <content><![CDATA[<p>Learnt Unix in NUS CS2030S. Here's a cheatsheet. <span id="more"></span></p>
<p><strong><a
href="https://nus-cs2030s.github.io/2324-s2/unix/background.html">Original
Text Here</a></strong></p>
<h1 id="background">Background</h1>
<ul>
<li><p><code>Ctrl + D</code> : end of input/ exit shell</p></li>
<li><p><code>Ctrl + Z</code> : suspend the current running program. This
pauses the execution of the program (but not terminates it). Resume
executing in the background with the command <code>bg</code> Back to
execution in the foreground again with the command
<code>fg</code>.</p></li>
<li><p><code>Ctrl + C</code> : terminate the current running
program.</p></li>
<li><p><code>Ctrl + S</code> : freeze the terminal. <code>Ctrl +
Q</code> : resume the terminal.</p></li>
<li><p><code>Ctrl + P</code> &amp; <code>Ctrl + N</code>: See the
previously executed commands.</p></li>
<li><p><code>Tab</code> : complete the name of a command/file</p></li>
<li><p><code>man</code> : look at the manuals. e.g. :
<code>man ls</code></p></li>
</ul>
<h1 id="essentials">Essentials</h1>
<h2 id="directory">Directory</h2>
<h3 id="symbols">Symbols</h3>
<ul>
<li><p><code>~</code> : The home directory. <code>~bob</code> means the
home directory of a user named bob.</p></li>
<li><p><code>.</code> : Current working directory.</p></li>
<li><p><code>/</code>: Root directory.</p></li>
<li><p><code>..</code> : Parent directory.</p></li>
</ul>
<h3 id="path">Path</h3>
<ul>
<li><p>Absolute Path: <code>/home/o/ooiwt</code></p></li>
<li><p>Relative Path: <code>../o/ooiwt</code></p></li>
<li><p><code>pwd</code> : Print current working directory.</p></li>
<li><p><code>ls</code> : Check the list of contents. Contents that
starts with <code>.</code> are hidden, use <code>ls -a</code> to check
<em>all</em> contents. <code>ls -F</code> to see more information.
<code>ls -l</code> to see information including the size and
date.</p></li>
<li><p><code>mkdir</code> : make a directory</p></li>
<li><p><code>cd</code> : brings you to the home directory</p></li>
</ul>
<h2 id="files">Files</h2>
<ul>
<li><p><code>rmdir</code> : remove a subdirectory, it must be
empty.</p></li>
<li><p><code>cp</code> : <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ ls</span><br><span class="line">test.txt</span><br><span class="line">$ cp test.txt foo.txt</span><br><span class="line">$ ls</span><br><span class="line">test.txt foo.txt</span><br></pre></td></tr></table></figure> <code>cp -r</code> copys the
whole directory: <code>$ cp -r ../workshop .</code></p></li>
</ul>
<p>* <em>If there is an existing file with the same name, cp will
overwrite the existing file without warning.</em></p>
<ul>
<li><p><code>mv</code> : Rename a content. <figure class="highlight plaintext"><figcaption><span>ls</span></figcaption><table><tr><td class="code"><pre><span class="line">foo.txt test.txt</span><br><span class="line">$ mv foo.txt bar.txt</span><br><span class="line">$ ls</span><br><span class="line">bar.txt test.txt</span><br></pre></td></tr></table></figure></p>
<p>When the destination of mv is a directory, then it moves the source
to the destisnation</p>
<p><em>It's better to use mv -i as it asks if you want to overwrite a
file</em></p></li>
<li><p><code>rm</code> : removes the file.</p>
<p>*It's better to use <code>rm -i</code>.</p></li>
<li><p><code>cat</code> : Take a look at the content of a file.</p>
<p><code>less</code> : look at less content of a file.</p>
<pre><code>  In less, use `space` to move forward,  `b` to move back, `q` to quit.</code></pre></li>
</ul>
<h2 id="file-permission">File Permission</h2>
<h3 id="what">What:</h3>
<ul>
<li><p><code>r</code> : read - 4</p></li>
<li><p><code>w</code> : write - 2</p></li>
<li><p><code>x</code> : executing a file / access contents and info of
the directory - 1</p></li>
</ul>
<p>Permission are used in two ways:</p>
<ol type="1">
<li><p>rwx or r-x etc.</p></li>
<li><p>Numerical notation like : 5 = r-x</p></li>
</ol>
<h3 id="who">Who</h3>
<p><code>u</code> is the user who owns the file; <code>g</code> refers
to the users in the same group as the user; and <code>o</code> are all
the other users.</p>
<p>For instance, the permission of <code>644</code>, or
<code>rw-r--r--</code>, on a file means that:</p>
<pre><code>- the owner can read and write

- the group users can only read

- all the other users can only read</code></pre>
<h3 id="check-and-change-of-permission">Check and Change of
Permission</h3>
<p>Check:</p>
<pre><code>$ ls -l test.txt
-rw-r--r--@ 1 ooiwt  staff  64 Jul 28 09:52 test.txt</code></pre>
<p>Change:</p>
<pre><code>$ chmod 666 test.txt
$ ls -l test.txt
-rw-rw-rw-@ 1 ooiwt  staff  64 Jul 28 09:52 test.txt</code></pre>
<p>Or:</p>
<pre><code>$ chmod o-w test.txt
$ ls -l test.txt
-rw-rw-r--@ 1 ooiwt  staff  64 Jul 28 09:52 test.txt</code></pre>
<p>Eliminates the writing permission of a user.(use o+w to give the
permission)</p>
<h2 id="standard-input-output">Standard Input &amp; Output</h2>
<ul>
<li><code>wc</code> : it will count the number of lines, words, and
characters in that given file. e.g.:</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wc text.txt</span><br><span class="line">$ wc test.txt</span><br><span class="line">1      11      64 test.txt</span><br></pre></td></tr></table></figure>
<ul>
<li><code>&gt;</code> &amp; <code>&gt;&gt;</code> : The operators &gt;
and &gt;&gt; are used to redirect the standard output to a file. The
difference is that &gt; will overwrite the given file, while &gt;&gt;
will concatenate into the given file. e.g.: <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ wc test.txt &gt; test.count</span><br><span class="line">$ cat test.count</span><br><span class="line">1      11      64 test.txt</span><br></pre></td></tr></table></figure></li>
</ul>
<p>This programme reads the output of <code>wc text.txt</code> into a
file test.count</p>
<ul>
<li><code>&lt;</code> &amp; <code>&lt;&lt;</code> : Imports the input.
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ wc test.txt</span><br><span class="line">       1      11      64 test.txt</span><br><span class="line">$ wc &lt; test.txt // This is reading the content from test.txt, thus the input is not a file but the content of test.txt.</span><br><span class="line">       1      11      64</span><br><span class="line">$ cat test.txt</span><br><span class="line">This is a test file for learning Unix file management commands.</span><br><span class="line">$ cat &lt; test.txt</span><br><span class="line">This is a test file for learning Unix file management commands.</span><br></pre></td></tr></table></figure></li>
</ul>
<h1 id="advanced">Advanced</h1>
<p>To be continued.</p>
]]></content>
      <tags>
        <tag>ComputerScience</tag>
        <tag>Unix</tag>
        <tag>Notes</tag>
        <tag>Study</tag>
        <tag>NUS</tag>
        <tag>CheatSheet</tag>
      </tags>
  </entry>
  <entry>
    <title>Thought - 2024.1</title>
    <url>/2024/01/23/Thought-2024-1-23/</url>
    <content><![CDATA[<p>Thougts in Jan 2024 <span id="more"></span></p>
<blockquote>
<p>1.23 : Sleep is important.</p>
</blockquote>
<blockquote>
<p>1.24 : 有舍有得，但也需分轻重。</p>
</blockquote>
<blockquote>
<p>1.25 : 在图书馆而非宿舍学习。</p>
</blockquote>
<blockquote>
<p>1.26 : 允许自己犯错。</p>
</blockquote>
<blockquote>
<p>1.27 : Push yourself.</p>
</blockquote>
<blockquote>
<p>1.28 : Try everything!</p>
</blockquote>
<blockquote>
<p>1.29 : YOLO</p>
</blockquote>
<blockquote>
<p>1.30 : 严于律己，宽于待人。</p>
</blockquote>
<blockquote>
<p>1.31 :...So you have to trust that the dots will somehow connect in
your future. You have to trust in something - your gut, destiny, life,
karma, whatever. - Steve Jobs</p>
</blockquote>
]]></content>
      <categories>
        <category>Thoughts</category>
      </categories>
      <tags>
        <tag>Thoughts</tag>
      </tags>
  </entry>
  <entry>
    <title>Notes for SICPJS</title>
    <url>/2024/01/23/Notes-for-SICPJS/</url>
    <content><![CDATA[<p>Notes for SICP JS. <span id="more"></span></p>
<h1 id="introduction">Introduction</h1>
<p><a href="https://sourceacademy.org/sicpjs">SICPJS</a> is a Java
Script version(or Source Language) of the computer science textbook
<em>Structure and Interpretation of Computer Programs (SICP)</em> . The
original textbook was written by Massachusetts Institute of Technology
professors Harold Abelson and Gerald Jay Sussman with Julie Sussman, and
is known as the "Wizard Book" in hacker culture. In NUS, year1 CS
students who take CS1101S uses SICPJS as the reference book on their
amazing study journey with the Source Academy.</p>
<p>During my study of CS in 2023/24 Sem 1 in CS1101, I find SICPJS a
great book. It not only works as an introductory book to computer
science, but also inspired me to think about the fundamental principles
of CS. I wrote this note during my sem1 study in CS1101S and is still
working on the book after that. Hopefully, this note would have been
finished in 2023/24 Sem2.</p>
<p>This note involves ideas that I find inspiring in the book and
questions that I came up with while reading. Notes with (U) are
questions that are unsolved and it would be great if readers could
comment below about your ideas on the notes.</p>
<p>I recommend anyone studying CS to read the book(either the original
version or JS version), and hopefully this note would be of some use to
the reader's understanding of the book.</p>
<h1 id="notes">Notes</h1>
<h2 id="foreword">Foreword</h2>
<ol type="1">
<li><p><strong>(U)What is combinatory calculus &amp; lambda calculus
&amp; predicate calculus?</strong></p>
<p><img src="1.png" /></p>
<p>Lambda Calculus : (lambda x.lambda y. x+y)(5 6) = 11</p>
<p>A lambda notation is equivalent to a Turing Machine, vice
versa</p></li>
<li><p><strong>(U)How can you use a lambda notation to represent AND and
OR?</strong></p>
<p>For reference, if we define True and False as:</p>
<pre><code> True: lambda x. lambda y. x

 False: lambda x. lambda y. y</code></pre>
<p>For any boolean value b, the logic NOT can be represented as</p>
<pre><code> NOT: lambda b. b False True</code></pre>
<p>Proof: case 1: b = True</p>
<pre><code>         Not b = Not True = True Flase True = False

     case2: b = False

         Not b = Not False = False False True = True</code></pre></li>
<li><p><strong>(U)What is object oriented approach?</strong></p></li>
<li><p><strong>(U)Why does the author say that most programming
languages “does not have a completely automatic storage
management”?</strong></p></li>
<li><p><strong>(U)Is it better to have 100 functions programmed on 1
data structure or 10 functions for each of 10 data
structures?</strong></p></li>
<li><p><strong>(U)What is Lisp?</strong></p></li>
</ol>
<h2 id="chapter-1-building-abstractions-with-functions">Chapter 1
Building Abstractions with Functions</h2>
<h3 id="section">1.1</h3>
<p><strong>Inflix Notation</strong></p>
<p>operand + operator + operand</p>
<p><strong>JS programmers know the value of everything but the cost of
nothing.(LOL)</strong></p>
<p>The fact that every expression has a value and JS programmers are
known for not caring about time/space efficiency.</p>
<p><strong>(U)What is the difference between statements and
expressions?</strong></p>
<p>A?</p>
<p><strong>Snake Case &amp; Camel Case</strong></p>
<p>Different ways to name variables.</p>
<h2 id="chapter-3-modularity-objects-and-state">Chapter 3: Modularity,
Objects, and State</h2>
<h3 id="section-1">3.1</h3>
<p><strong>Why use the CSE machine, why is environment an important
thing?(3.1.2)</strong></p>
<p>Storing the temporary data that the function needs in the local
environment of a function helps us to generalize functions(for example,
in this way the memoization progress can be applied to different
functions)</p>
<p>Encapsulation: We say a variable is encapsulated in the environment
when its information is only accessed inside the environment. A general
systemword designing principle called “the hiding principle“ is that a
system gets more modular and robust by providing information access to
only the parts of the programme that are “need to know”. </p>
<p><strong>Monte Carlo Method</strong></p>
<p>The use of randomness. Say you want to calculate the area of a
circle(x^2 +y^2 &lt;= 1). Choose a rectangle(whose area you have already
know), and try to make many following experiments: pick up one point and
test whether the point is in the circle. If we try this many times, we
get the possibility of a point in the rectangle is in the circle, thus p
* area_of_rectangle = area_of_circle.</p>
<p> </p>
]]></content>
      <categories>
        <category>Notes</category>
        <category>CS</category>
        <category>Overview</category>
      </categories>
      <tags>
        <tag>ComputerScience</tag>
        <tag>Notes</tag>
        <tag>Study</tag>
        <tag>NUS</tag>
        <tag>JavaScript</tag>
        <tag>CS1101S</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2024/01/20/hello-world/</url>
    <content><![CDATA[<p>This is a Blog to test this website's functionalities.
<span id="more"></span></p>
<hr />
<h3 id="url">URL</h3>
<p><a href="https://wch.github.io/latexsheet/latexsheet.pdf">markdown
cheatsheet</a></p>
<p><a href="https://wch.github.io/latexsheet/latexsheet.pdf">latex
Cheatsheet</a></p>
<h3 id="code">code</h3>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Hello World!</span><br></pre></td></tr></table></figure>
<h3 id="useful-syntax">useful syntax</h3>
<p>Here's a sentence with a footnote. <a href="#fn1"
class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<blockquote>
<p>This is a quote.</p>
</blockquote>
<ul class="task-list">
<li><label><input type="checkbox" checked="" />Write the press
release</label></li>
<li><label><input type="checkbox" />Update the website</label></li>
<li><label><input type="checkbox" />Contact the media</label></li>
</ul>
<p><span class="math inline">\(\color{blue}{blue}\)</span></p>
<h3 id="create-a-new-post">Create a new post</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>
<p>More info: <a
href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="mathjax-testing">mathjax testing</h3>
<p><span class="math display">\[\begin{equation}
   E = mc^2
\end{equation}\]</span></p>
<p><span class="math display">\[\begin{equation}
\begin{aligned}
a &amp;= b + c \\
  &amp;= d + e + f + g \\
  &amp;= h + i
\end{aligned}
\end{equation}\]</span></p>
<p><span class="math display">\[x+1\over\sqrt{1-x^2} \]</span></p>
<section id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p>This is the footnote.<a href="#fnref1"
class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>
]]></content>
  </entry>
</search>
